# SynPharm 架构设计文档

> 文档版本：v1.0.0  
> 创建日期：2026-07-26  
> 文档状态：正式版

---

## 目录

1. [系统架构图](#1-系统架构图)
2. [登录流程时序图](#2-登录流程时序图)
3. [预测流程时序图](#3-预测流程时序图)
4. [批量预测流程时序图](#4-批量预测流程时序图)
5. [管道机制类图](#5-管道机制类图)
6. [数据库ER图](#6-数据库er图)
7. [Docker容器架构图](#7-docker容器架构图)
8. [安全过滤链流程图](#8-安全过滤链流程图)
9. [Bean依赖关系图](#9-bean依赖关系图)

---

## 1. 系统架构图

```plantuml
@startuml
skinparam backgroundColor #FEFEFE
skinparam componentStyle rectangle
skinparam classStyle rectangle

title SynPharm 系统架构图

package "前端层" {
  [Vue 3 + Vite] as Frontend
  [Element Plus] as UI
  [Axios] as HTTP
}

package "业务中台" {
  [Spring Boot] as Backend
  [Controller层] as Controller
  [Service层] as Service
  [Pipeline层] as Pipeline
  [Client层] as Client
}

package "算法引擎" {
  [FastAPI] as FastAPI
  [API路由层] as Router
  [算法服务层] as AlgoService
  [PyTorch模型] as Model
}

package "数据层" {
  [MySQL] as MySQL
  [Redis] as Redis
  [文件系统] as FS
}

Frontend --> Controller : HTTP请求
Controller --> Service : 业务处理
Service --> Pipeline : 管道调用
Pipeline --> Client : HTTP调用
Client --> Router : HTTP请求
Router --> AlgoService : 算法执行
AlgoService --> Model : 模型推理

Service --> MySQL : 数据持久化
Service --> Redis : 缓存/限流
Backend --> FS : 文件上传/下载

@enduml
```

**架构说明**：

| 层级 | 技术 | 职责 |
|:---|:---|:---|
| 前端层 | Vue 3 + Element Plus | 用户界面展示，交互逻辑 |
| 业务中台 | Spring Boot | 业务逻辑、管道编排、安全认证 |
| 算法引擎 | FastAPI + PyTorch | 纯算法运算，GPU加速推理 |
| 数据层 | MySQL + Redis + 文件系统 | 数据存储、缓存、文件管理 |

---

## 2. 登录流程时序图

```plantuml
@startuml
title 用户登录流程时序图

actor "用户" as User
participant "前端" as Frontend
participant "AuthController" as Controller
participant "AuthService" as Service
participant "LoginStrategyFactory" as Factory
participant "QqEmailLoginStrategy" as Strategy
participant "JwtUtils" as Jwt
participant "Redis" as Redis
participant "MySQL" as MySQL

User -> Frontend: 点击"发送验证码"
Frontend -> Controller: POST /api/auth/captcha/send
Controller -> Service: captchaService.sendCaptcha(email, type)
Service -> Redis: GET captcha:email:limit:{email}
Redis --> Service: 返回计数
Service -> Redis: SET captcha:email:{email} {code} EX 60
Service -> Service: 发送邮件
Service --> Controller: 返回成功
Controller --> Frontend: 200 OK
Frontend --> User: 提示"验证码已发送"

User -> Frontend: 输入验证码，点击"登录"
Frontend -> Controller: POST /api/auth/login
Controller -> Service: authService.login(request, httpRequest)
Service -> Redis: GET login:lock:{account}
Redis --> Service: 返回nil
Service -> Factory: login("qq_email", request, httpRequest)
Factory -> Strategy: login(request, httpRequest)
Strategy -> Redis: GET captcha:email:{email}
Redis --> Strategy: 返回验证码
Strategy -> Strategy: 验证验证码
Strategy -> MySQL: SELECT * FROM sys_user WHERE email
MySQL --> Strategy: 返回用户(或空)
alt 用户存在
  Strategy --> Factory: 返回用户信息
else 用户不存在
  Strategy -> MySQL: INSERT INTO sys_user
  MySQL --> Strategy: 返回新用户
end
Factory --> Service: LoginResponse
Service -> Jwt: generateToken(userId, email, role)
Jwt --> Service: 返回Token
Service -> MySQL: INSERT INTO sys_login_log
Service --> Controller: LoginResponse
Controller --> Frontend: 200 OK {user, token}
Frontend -> Frontend: 保存Token到localStorage
Frontend --> User: 登录成功

@enduml
```

---

## 3. 预测流程时序图

```plantuml
@startuml
title 单条预测流程时序图

actor "用户" as User
participant "前端" as Frontend
participant "JwtFilter" as JwtFilter
participant "PredictController" as Controller
participant "PredictService" as Service
participant "DataPipelineFactory" as Factory
participant "SmilesInputParser" as Parser
participant "DtiAlgoExecutor" as Executor
participant "FastApiClient" as Client
participant "FastAPI" as FastAPI
participant "JsonOutputFormatter" as Formatter

User -> Frontend: 填写预测参数并提交
Frontend -> JwtFilter: POST /api/predict/general
note right: Authorization: Bearer {token}
JwtFilter -> JwtFilter: 验证Token
JwtFilter -> JwtFilter: 检查黑名单
JwtFilter -> Controller: 放行请求
Controller -> Service: predict(request, userId)
Service -> Factory: process("smiles", "DTI", "json", inputValue, null)
Factory -> Parser: parse(inputValue, null)
Parser -> Parser: 解析SMILES输入
Parser --> Factory: ParsedInput
Factory -> Executor: execute(parsedInput)
Executor -> Client: predictSingle(request)
Client -> FastAPI: POST /v1/predict/single
FastAPI -> FastAPI: 路由匹配(algo_type=DTI)
FastAPI -> FastAPI: 调用DTIService.predict()
FastAPI --> Client: AlgoResponse
Client --> Executor: AlgoResponse
Executor --> Factory: AlgoResponse
Factory -> Formatter: format(algoResponse)
Formatter -> Formatter: 格式化结果
Formatter --> Factory: PredictResultResponse
Factory --> Service: PredictResultResponse
Service --> Controller: PredictResultResponse
Controller --> JwtFilter: 200 OK
JwtFilter --> Frontend: 200 OK
Frontend --> User: 展示预测结果

@enduml
```

---

## 4. 批量预测流程时序图

```plantuml
@startuml
title 批量预测流程时序图

actor "用户" as User
participant "前端" as Frontend
participant "BatchController" as Controller
participant "BatchProcessService" as Service
participant "线程池" as ThreadPool
participant "DataPipelineFactory" as Factory
participant "Redis" as Redis
participant "MySQL" as MySQL
participant "FastAPI" as FastAPI

User -> Frontend: 上传CSV文件
Frontend -> Controller: POST /api/batch/upload
Controller -> Service: uploadBatch(file, algoType, userId)
Service -> Service: 保存文件到uploads/
Service -> Service: 统计CSV行数
Service -> MySQL: INSERT INTO batch_task
MySQL --> Service: 返回任务ID
Service -> Redis: SET batch:progress:{batchId} {json}
Service -> ThreadPool: @Async submitBatchTask(batchId, algoType)
Service --> Controller: BatchUploadResponse
Controller --> Frontend: 200 OK {batchId, status: PENDING}
Frontend --> User: 显示任务ID和进度

note over ThreadPool: 异步线程开始执行

ThreadPool -> Service: processBatch(batchId, algoType)
Service -> Service: 读取CSV文件
Service -> Service: 按CHUNK_SIZE=50分割
loop 每个Chunk
  Service -> Factory: batchProcess(inputType, algoType, outputType, chunk)
  Factory -> Factory: 批量解析输入
  Factory -> FastAPI: POST /v1/predict/batch
  FastAPI --> Factory: BatchPredictionResponse
  Factory -> Factory: 批量格式化输出
  Factory --> Service: List<PredictResultResponse>
  Service -> Redis: SET batch:progress:{batchId} {progress: 60%}
  Service -> MySQL: UPDATE batch_task SET progress=60%
end
Service -> Service: 生成结果CSV文件
Service -> MySQL: UPDATE batch_task SET status=2, resultPath=xxx
Service -> Redis: SET batch:progress:{batchId} {progress: 100%}

note over Frontend: 用户轮询查询进度

Frontend -> Controller: GET /api/batch/{batchId}
Controller -> Service: getBatchStatus(batchId)
Service -> Redis: GET batch:progress:{batchId}
Redis --> Service: 返回进度信息
Service --> Controller: BatchStatusResponse
Controller --> Frontend: 200 OK {progress: 100%, status: COMPLETED}
Frontend --> User: 显示完成，提供下载链接

@enduml
```

---

## 5. 管道机制类图

```plantuml
@startuml
title 管道机制类图

skinparam backgroundColor #FEFEFE
skinparam classStyle rectangle

interface InputParser {
  +getInputType(): InputType
  +parse(inputValue: String, fileUrl: String): ParsedInput
}

interface AlgoExecutor {
  +getAlgoType(): AlgoType
  +execute(inputData: ParsedInput): AlgoResponse
  +batchExecute(inputDataList: List<ParsedInput>): List<AlgoResponse>
}

interface OutputFormatter {
  +getOutputType(): OutputType
  +format(resultData: AlgoResponse): PredictResultResponse
  +batchFormat(resultDataList: List<AlgoResponse>): List<PredictResultResponse>
}

interface PipelineFactory {
  +process(inputType, algoType, outputType, inputValue, fileUrl): PredictResultResponse
  +batchProcess(inputType, algoType, outputType, inputs): List<PredictResultResponse>
  +supports(inputType, algoType, outputType): boolean
}

class DataPipelineFactory {
  -parserMap: Map<InputType, InputParser>
  -executorMap: Map<AlgoType, AlgoExecutor>
  -formatterMap: Map<OutputType, OutputFormatter>
  +process(inputType, algoType, outputType, inputValue, fileUrl): PredictResultResponse
  +batchProcess(inputType, algoType, outputType, inputs): List<PredictResultResponse>
  +supports(inputType, algoType, outputType): boolean
}

class SmilesInputParser {
  +getInputType(): InputType
  +parse(inputValue: String, fileUrl: String): ParsedInput
}

class UniprotInputParser {
  +getInputType(): InputType
  +parse(inputValue: String, fileUrl: String): ParsedInput
}

class PdbInputParser {
  +getInputType(): InputType
  +parse(inputValue: String, fileUrl: String): ParsedInput
}

class CsvInputParser {
  +getInputType(): InputType
  +parse(inputValue: String, fileUrl: String): ParsedInput
}

class DtiAlgoExecutor {
  -fastApiClient: FastApiClient
  +getAlgoType(): AlgoType
  +execute(inputData: ParsedInput): AlgoResponse
  +batchExecute(inputDataList: List<ParsedInput>): List<AlgoResponse>
}

class PpiAlgoExecutor {
  -fastApiClient: FastApiClient
  +getAlgoType(): AlgoType
  +execute(inputData: ParsedInput): AlgoResponse
  +batchExecute(inputDataList: List<ParsedInput>): List<AlgoResponse>
}

class DdiAlgoExecutor {
  -fastApiClient: FastApiClient
  +getAlgoType(): AlgoType
  +execute(inputData: ParsedInput): AlgoResponse
  +batchExecute(inputDataList: List<ParsedInput>): List<AlgoResponse>
}

class JsonOutputFormatter {
  +getOutputType(): OutputType
  +format(resultData: AlgoResponse): PredictResultResponse
  +batchFormat(resultDataList: List<AlgoResponse>): List<PredictResultResponse>
}

class CsvOutputFormatter {
  +getOutputType(): OutputType
  +format(resultData: AlgoResponse): PredictResultResponse
  +batchFormat(resultDataList: List<AlgoResponse>): List<PredictResultResponse>
}

DataPipelineFactory ..|> PipelineFactory

SmilesInputParser ..|> InputParser
UniprotInputParser ..|> InputParser
PdbInputParser ..|> InputParser
CsvInputParser ..|> InputParser

DtiAlgoExecutor ..|> AlgoExecutor
PpiAlgoExecutor ..|> AlgoExecutor
DdiAlgoExecutor ..|> AlgoExecutor

JsonOutputFormatter ..|> OutputFormatter
CsvOutputFormatter ..|> OutputFormatter

DataPipelineFactory o-- InputParser : contains
DataPipelineFactory o-- AlgoExecutor : contains
DataPipelineFactory o-- OutputFormatter : contains

DtiAlgoExecutor --> FastApiClient : uses
PpiAlgoExecutor --> FastApiClient : uses
DdiAlgoExecutor --> FastApiClient : uses

@enduml
```

**设计说明**：

| 组件 | 职责 | 设计模式 |
|:---|:---|:---|
| InputParser | 解析不同类型的输入数据 | 策略模式 |
| AlgoExecutor | 执行不同算法的预测 | 策略模式 |
| OutputFormatter | 格式化不同类型的输出 | 策略模式 |
| DataPipelineFactory | 动态组合策略，执行管道 | 工厂模式 |

---

## 6. 数据库ER图

```plantuml
@startuml
title 数据库ER图

entity sys_user {
  * id: BIGINT <<PK>>
  --
  * email: VARCHAR(100) <<UK>>
  - password: VARCHAR(255)
  - nickname: VARCHAR(50)
  - role: VARCHAR(20)
  - status: TINYINT
  - created_at: DATETIME
  - updated_at: DATETIME
}

entity sys_login_log {
  * id: BIGINT <<PK>>
  --
  * user_id: BIGINT <<FK>>
  - account: VARCHAR(100)
  - login_type: VARCHAR(20)
  - ip: VARCHAR(50)
  - user_agent: VARCHAR(500)
  - success: TINYINT
  - error_msg: VARCHAR(500)
  - created_at: DATETIME
}

entity predict_task {
  * id: BIGINT <<PK>>
  --
  * user_id: BIGINT <<FK>>
  - input_type: VARCHAR(20)
  - algo_type: VARCHAR(20)
  - input_data: TEXT
  - status: TINYINT
  - created_at: DATETIME
}

entity predict_result {
  * id: BIGINT <<PK>>
  --
  * task_id: BIGINT <<FK>>
  - algo_type: VARCHAR(20)
  - result_data: TEXT
  - created_at: DATETIME
}

entity batch_task {
  * batch_id: VARCHAR(36) <<PK>>
  --
  * user_id: BIGINT <<FK>>
  - file_path: VARCHAR(500)
  - total_count: INT
  - success_count: INT
  - fail_count: INT
  - progress: DECIMAL(5,2)
  - status: TINYINT
  - result_path: VARCHAR(500)
  - created_at: DATETIME
  - updated_at: DATETIME
}

entity user_favorite {
  * id: BIGINT <<PK>>
  --
  * user_id: BIGINT <<FK>>
  * task_id: BIGINT <<FK>>
  - created_at: DATETIME
}

sys_user ||--o{ sys_login_log : "has"
sys_user ||--o{ predict_task : "creates"
sys_user ||--o{ batch_task : "creates"
sys_user ||--o{ user_favorite : "favorites"
predict_task ||--o{ predict_result : "has"
predict_task ||--o{ user_favorite : "is favorited by"

@enduml
```

---

## 7. Docker容器架构图

```plantuml
@startuml
title Docker容器架构图

skinparam backgroundColor #FEFEFE

package "synpharm-network (172.20.0.0/16)" {
  node "MySQL" as MySQL {
    artifact "mysql:8.0" as MySQLImage
    artifact "mysql_data" as MySQLVolume
  }
  
  node "Redis" as Redis {
    artifact "redis:7-alpine" as RedisImage
    artifact "redis_data" as RedisVolume
  }
  
  node "FastAPI" as FastAPI {
    artifact "python:3.10-slim" as FastAPIImage
    artifact "fastapi_logs" as FastAPIVolume
  }
  
  node "Spring Boot" as Backend {
    artifact "openjdk:21-jdk-slim" as BackendImage
    artifact "backend_uploads" as UploadVolume
    artifact "backend_results" as ResultVolume
  }
}

cloud "主机端口映射" as Host {
  port "8080" as Port8080
  port "8000" as Port8000
  port "3306" as Port3306
  port "6379" as Port6379
}

Backend --> MySQL : jdbc:mysql://mysql:3306
Backend --> Redis : redis://redis:6379
Backend --> FastAPI : http://fastapi:8000

Backend --> Port8080 : 8080
FastAPI --> Port8000 : 8000
MySQL --> Port3306 : 3306
Redis --> Port6379 : 6379

@enduml
```

**容器通信说明**：

| 容器 | 内部地址 | 主机映射端口 | 用途 |
|:---|:---|:---|:---|
| synpharm-mysql | mysql:3306 | 3306 | 数据库服务 |
| synpharm-redis | redis:6379 | 6379 | 缓存服务 |
| synpharm-fastapi | fastapi:8000 | 8000 | 算法服务 |
| synpharm-backend | backend:8080 | 8080 | 业务服务 |

---

## 8. 安全过滤链流程图

```plantuml
@startuml
title Spring Security过滤链流程

start
:HTTP请求到达;

:WebAsyncManagerIntegrationFilter;
:处理异步请求上下文;

:SecurityContextPersistenceFilter;
:获取/存储SecurityContext;

:HeaderWriterFilter;
:添加安全响应头;

:CorsFilter;
if (是否是OPTIONS请求?) then (是)
  :处理预检请求;
else (否)
  :继续;
endif

:JwtAuthenticationFilter;
if (Authorization头存在?) then (是)
  :提取Token;
  if (Token有效?) then (是)
    :检查黑名单;
    if (不在黑名单?) then (是)
      :构建Authentication;
      :设置SecurityContext;
    else (在黑名单)
      :抛出TOKEN_BLACKLISTED异常;
      stop;
    endif
  else (无效)
    :抛出TOKEN_INVALID异常;
    stop;
  endif
else (否)
  :继续(匿名访问);
endif

:RequestCacheAwareFilter;
:处理请求缓存;

:SecurityContextHolderAwareRequestFilter;
:注入SecurityContext信息;

:AnonymousAuthenticationFilter;
if (无Authentication?) then (是)
  :创建匿名用户;
else (有)
  :继续;
endif

:SessionManagementFilter;
:STATELESS模式，跳过;

:ExceptionTranslationFilter;
:捕获Security异常;

:FilterSecurityInterceptor;
:权限判断;
if (有权限?) then (是)
  :放行到Controller;
else (无)
  :抛出AccessDeniedException;
  stop;
endif

:Controller处理请求;
:返回响应;
stop

@enduml
```

---

## 9. Bean依赖关系图

```plantuml
@startuml
title Spring Boot Bean依赖关系图

skinparam backgroundColor #FEFEFE

package "配置层" {
  [SecurityConfig] as SecConfig
  [AsyncConfig] as AsyncConfig
  [WebClientConfig] as WebClientConfig
  [CorsConfig] as CorsConfig
}

package "工具层" {
  [JwtUtils] as Jwt
  [CsvUtils] as Csv
}

package "数据访问层" {
  [SysUserMapper] as UserMapper
  [SysLoginLogMapper] as LogMapper
  [BatchTaskMapper] as BatchMapper
}

package "认证服务" {
  [EmailCaptchaServiceImpl] as CaptchaService
  [LoginStrategyFactory] as StrategyFactory
  [QqEmailLoginStrategy] as QqStrategy
  [GuestLoginStrategy] as GuestStrategy
  [AuthServiceImpl] as AuthService
}

package "预测服务" {
  [SmilesInputParser] as SmilesParser
  [DtiAlgoExecutor] as DtiExecutor
  [JsonOutputFormatter] as JsonFormatter
  [DataPipelineFactory] as PipelineFactory
  [FastApiClient] as FastApiClient
  [PredictServiceImpl] as PredictService
  [BatchProcessServiceImpl] as BatchService
}

package "控制器层" {
  [AuthController] as AuthCtrl
  [PredictController] as PredictCtrl
  [BatchController] as BatchCtrl
}

package "基础设施" {
  [StringRedisTemplate] as RedisTemplate
  [BCryptPasswordEncoder] as BCrypt
  [WebClient] as WebClientBean
}

SecConfig --> Jwt
SecConfig --> BCrypt

AsyncConfig --> AsyncConfig : 创建batchTaskExecutor

WebClientConfig --> WebClientBean : 创建fastApiWebClient

CaptchaService --> RedisTemplate
CaptchaService --> AuthService : uses

StrategyFactory --> QqStrategy : contains
StrategyFactory --> GuestStrategy : contains

AuthService --> StrategyFactory
AuthService --> Jwt
AuthService --> RedisTemplate
AuthService --> UserMapper
AuthService --> LogMapper
AuthService --> CaptchaService
AuthService --> BCrypt

SmilesParser --> SmilesParser : @Component

DtiExecutor --> FastApiClient

JsonFormatter --> JsonFormatter : @Component

PipelineFactory --> SmilesParser : contains
PipelineFactory --> DtiExecutor : contains
PipelineFactory --> JsonFormatter : contains

FastApiClient --> WebClientBean
FastApiClient --> WebClientConfig : timeout配置

PredictService --> PipelineFactory

BatchService --> BatchMapper
BatchService --> PipelineFactory
BatchService --> RedisTemplate

AuthCtrl --> AuthService
AuthCtrl --> CaptchaService

PredictCtrl --> PredictService
PredictCtrl --> Jwt

BatchCtrl --> BatchService

@enduml
```

---

## 附录：UML图查看方法

### 在线查看

将PlantUML代码复制到以下在线工具中查看：

- [PlantUML Online Server](https://www.plantuml.com/plantuml/)
- [Draw.io](https://app.diagrams.net/)

### 本地查看

安装PlantUML插件：

**IntelliJ IDEA / PyCharm**：
1. 安装插件：PlantUML Integration
2. 右键点击 `.md` 文件 → Diagrams → Show Diagram

**VS Code**：
1. 安装插件：PlantUML
2. 按 `Ctrl+Shift+P` → PlantUML: Preview Current Diagram

### 生成图片

```bash
# 安装PlantUML
sudo apt-get install plantuml

# 生成图片
plantuml architecture_diagram.puml -o output/
```