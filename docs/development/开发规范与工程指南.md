# 开发规范与工程指南

## 目录

1. [部署流程](#1-部署流程)
2. [Git分支策略与合并规范](#2-git分支策略与合并规范)
3. [分模块测试方案](#3-分模块测试方案)

---

## 1. 部署流程

### 1.1 部署前检查清单

| 检查项 | 说明 | 验证方法 |
|:---|:---|:---|
| 依赖版本 | Java 21+, Python 3.10+, MySQL 8.0+, Redis 7.0+ | `java -version`, `python --version`, `mysql --version`, `redis-cli ping` |
| 数据库配置 | 数据库已创建，用户权限正确 | `mysql -u synpharm -p -e "SELECT 1"` |
| Redis配置 | Redis可连接，无密码或密码正确 | `redis-cli ping` |
| 模型文件 | FastAPI models目录下有模型文件 | `ls synpharm-fastapi/models/` |
| 环境变量 | `.env` 文件配置正确 | 检查各模块 `.env` 文件 |
| 端口占用 | 8080/8000/80端口未被占用 | `netstat -tlnp \| grep 8080` |

### 1.2 部署步骤（开发环境）

#### 步骤1：启动基础服务

```bash
# 启动MySQL
systemctl start mysql
# 或 Docker
docker run -d --name mysql -p 3306:3306 -e MYSQL_ROOT_PASSWORD=synpharm_root mysql:8.0

# 启动Redis
systemctl start redis
# 或 Docker
docker run -d --name redis -p 6379:6379 redis:7-alpine
```

#### 步骤2：启动后端服务

```bash
cd synpharm-backend
mvn clean package -DskipTests
java -jar target/synpharm-backend-1.0.0.jar
```

验证：`curl http://localhost:8080/api/auth/captcha/send -X POST -H "Content-Type: application/json" -d '{"email":"test@qq.com","type":"login"}'`

#### 步骤3：启动FastAPI服务

```bash
cd synpharm-fastapi
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 8000
```

验证：`curl http://localhost:8000/health`

#### 步骤4：启动前端服务

```bash
cd synpharm-frontend
npm install
npm run dev
```

验证：访问 `http://localhost:5174`

### 1.3 部署步骤（生产环境 - Docker Compose）

```bash
# 1. 构建后端
cd synpharm-backend
mvn clean package -DskipTests

# 2. 构建前端
cd synpharm-frontend
npm install
npm run build

# 3. 启动所有服务（进入docker目录）
cd ../docker
docker compose up -d

# 4. 验证服务状态
docker compose ps
```

### 1.4 部署后验证清单

| 服务 | 验证方法 | 预期结果 |
|:---|:---|:---|
| 前端 | 访问 `http://服务器IP` | 登录页面正常显示 |
| 后端API | `curl http://服务器IP:8080/api/auth/captcha/send` | 返回200 |
| FastAPI | `curl http://服务器IP:8000/health` | 返回健康状态 |
| MySQL | `docker-compose exec mysql mysql -u synpharm -p -e "SELECT 1"` | 返回1 |
| Redis | `docker-compose exec redis redis-cli ping` | PONG |

### 1.5 部署流程图

```
┌─────────────────────────────────────────────────────────┐
│                    部署流程                             │
├─────────────────────────────────────────────────────────┤
│                                                        │
│  ┌─────────────────┐                                   │
│  │ 1. 环境准备     │                                   │
│  │ - 安装依赖      │                                   │
│  │ - 配置环境变量  │                                   │
│  └────────┬────────┘                                   │
│           ▼                                            │
│  ┌─────────────────┐                                   │
│  │ 2. 启动基础服务 │                                   │
│  │ - MySQL         │                                   │
│  │ - Redis         │                                   │
│  └────────┬────────┘                                   │
│           ▼                                            │
│  ┌─────────────────┐     ┌─────────────────┐           │
│  │ 3. 启动后端     │     │ 3. 启动FastAPI │           │
│  │ Spring Boot     │     │ Python服务      │           │
│  └────────┬────────┘     └────────┬────────┘           │
│           │                       │                    │
│           └───────────┬───────────┘                    │
│                       ▼                                │
│  ┌───────────────────────────────────┐                 │
│  │ 4. 启动前端（Nginx反向代理）      │                 │
│  │ - Vue开发模式 / Nginx生产模式     │                 │
│  └───────────────────────────────────┘                 │
│                                                        │
└─────────────────────────────────────────────────────────┘
```

---

## 2. Git分支策略与合并规范

### 2.1 分支结构

```
main                    ← 生产分支（稳定代码）
├── develop             ← 开发分支（集成所有功能）
│   ├── feature/ppi-algo-executor     ← 功能分支
│   ├── feature/ddi-algo-executor     ← 功能分支
│   ├── feature/uniprot-input-parser  ← 功能分支
│   ├── fix/auth-bug                  ← 修复分支
│   └── hotfix/紧急修复               ← 热修复分支
```

### 2.2 分支命名规范

| 分支类型 | 命名格式 | 示例 |
|:---|:---|:---|
| 功能开发 | `feature/{模块}-{功能}` | `feature/ppi-algo-executor`, `feature/uniprot-input-parser` |
| Bug修复 | `fix/{模块}-{问题描述}` | `fix/auth-captcha-error`, `fix/pipeline-batch-process` |
| 热修复 | `hotfix/{问题描述}` | `hotfix/login-failure`, `hotfix/api-timeout` |
| 重构 | `refactor/{模块}-{描述}` | `refactor/auth-unify-login`, `refactor/pipeline-factory` |
| 文档 | `docs/{描述}` | `docs/deployment-guide`, `docs/api-spec` |

### 2.3 分支创建与合并流程

#### 2.3.1 功能开发完整流程

```
┌─────────────────────────────────────────────────────────┐
│               功能开发完整流程                           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  步骤1: 准备工作                                        │
│  ├── 切换到develop分支                                  │
│  ├── 拉取最新代码                                       │
│  └── 确认本地环境正常                                   │
│                                                         │
│  步骤2: 创建分支                                        │
│  ├── 按照命名规范创建功能分支                            │
│  └── 验证分支创建成功                                   │
│                                                         │
│  步骤3: 开发代码                                        │
│  ├── 编写实现代码                                       │
│  ├── 编写单元测试                                       │
│  ├── 本地测试验证                                       │
│  └── 多次小步提交                                       │
│                                                         │
│  步骤4: 提交代码                                        │
│  ├── 检查代码变更                                       │
│  ├── 按规范编写提交信息                                 │
│  └── 推送到远程仓库                                     │
│                                                         │
│  步骤5: 创建PR                                          │
│  ├── 在GitHub创建Pull Request                           │
│  ├── 填写PR描述和测试说明                               │
│  └── 请求代码审查                                       │
│                                                         │
│  步骤6: 代码审查与合并                                   │
│  ├── 审查反馈修改                                       │
│  ├── 审查通过                                           │
│  └── 合并到develop分支                                  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### 2.3.2 功能开发详细步骤

```bash
# ==========================================
# 步骤1：从develop拉取最新代码
# ==========================================
git checkout develop
git pull origin develop

# 验证：查看当前分支和最新提交
git branch -v
git log --oneline -5

# ==========================================
# 步骤2：创建功能分支
# ==========================================
# 命名格式: feature/{模块}-{功能}
git checkout -b feature/ppi-algo-executor

# 验证：确认在新分支上
git branch
# 输出:
#   develop
# * feature/ppi-algo-executor

# ==========================================
# 步骤3：开发代码（多次提交）
# ==========================================
# 第一次提交：核心实现
git add .
git status  # 检查要提交的文件
git commit -m "feat: pipeline 添加PPI算法执行器

- 实现PpiAlgoExecutor类
- 实现execute和batchExecute方法
- 调用FastAPI执行PPI预测"

# 第二次提交：单元测试
git add .
git commit -m "test: pipeline 添加PPI算法执行器单元测试

- 添加PpiAlgoExecutorTest
- 覆盖正常流程和异常场景"

# ==========================================
# 步骤4：推送到远程
# ==========================================
git push origin feature/ppi-algo-executor

# 验证：查看远程分支
git branch -r
# 输出:
#   origin/develop
#   origin/feature/ppi-algo-executor
#   origin/main

# ==========================================
# 步骤5：在GitHub创建PR（Pull Request）
# ==========================================
# 1. 打开GitHub项目页面
# 2. 点击 "Compare & pull request"
# 3. 填写PR信息:
#    - Title: feat: pipeline 添加PPI算法执行器
#    - Description:
#      ## 功能描述
#      添加PPI算法执行器，支持单条和批量预测
#
#      ## 测试说明
#      - [x] 单元测试通过
#      - [x] 本地接口测试通过
#      - [x] 批量测试通过
#
# ## 关联文档
# 参考: docs/modules/predict/algorithm/PPI算法开发操作文档.md
#
# 4. 目标分支：develop
# 5. 点击 "Create pull request"

# ==========================================
# 步骤6：代码审查通过后合并到develop
# ==========================================
# 审查通过后，在GitHub点击 "Merge pull request"
# 选择 "Squash and merge"（将多个提交压缩为一个）
# 合并后删除远程分支

# 本地同步
git checkout develop
git pull origin develop

# 删除本地功能分支（可选）
git branch -d feature/ppi-algo-executor
```

#### 2.3.3 Bug修复流程

```bash
# 1. 从develop拉取最新代码
git checkout develop
git pull origin develop

# 2. 创建修复分支
# 命名格式: fix/{模块}-{问题描述}
git checkout -b fix/auth-captcha-error

# 3. 修复代码
git add .
git commit -m "fix: auth 修复验证码发送失败问题

- 修复CaptchaService中邮件发送逻辑
- 添加异常处理和日志"

# 4. 推送并创建PR
git push origin fix/auth-captcha-error

# 5. 创建PR，目标分支：develop
```

#### 2.3.4 热修复流程（紧急问题）

```bash
# 1. 从main拉取代码（直接从生产分支创建）
git checkout main
git pull origin main

# 2. 创建热修复分支
# 命名格式: hotfix/{问题描述}
git checkout -b hotfix/login-failure

# 3. 修复代码
git add .
git commit -m "hotfix: 紧急修复登录接口500错误

- 修复JWT解析异常
- 添加降级处理"

# 4. 推送到远程并创建PR
git push origin hotfix/login-failure

# 5. 创建PR，目标分支：main
# 6. 合并到main后，再创建PR合并到develop
```

#### 2.3.5 常用Git命令速查

| 命令 | 说明 | 示例 |
|:---|:---|:---|
| `git branch` | 查看本地分支 | `git branch` |
| `git branch -r` | 查看远程分支 | `git branch -r` |
| `git branch -a` | 查看所有分支 | `git branch -a` |
| `git checkout {branch}` | 切换分支 | `git checkout develop` |
| `git checkout -b {branch}` | 创建并切换分支 | `git checkout -b feature/xxx` |
| `git status` | 查看文件状态 | `git status` |
| `git add .` | 添加所有文件 | `git add .` |
| `git add {file}` | 添加指定文件 | `git add src/...` |
| `git commit -m "{msg}"` | 提交代码 | `git commit -m "feat: ..."` |
| `git push origin {branch}` | 推送到远程 | `git push origin feature/xxx` |
| `git pull origin {branch}` | 拉取远程代码 | `git pull origin develop` |
| `git log --oneline -10` | 查看提交历史 | `git log --oneline -10` |
| `git diff` | 查看未暂存的变更 | `git diff` |
| `git diff --staged` | 查看已暂存的变更 | `git diff --staged` |
| `git branch -d {branch}` | 删除本地分支 | `git branch -d feature/xxx` |
| `git stash` | 暂存当前修改 | `git stash` |
| `git stash pop` | 恢复暂存的修改 | `git stash pop` |

### 2.4 提交信息规范

#### 格式

```
{类型}: {模块} {功能描述}

{详细说明（可选）}

{关联的Issue或PR编号}
```

#### 类型说明

| 类型 | 说明 |
|:---|:---|
| `feat` | 新功能（新增接口、新增类、新增功能） |
| `fix` | 修复Bug |
| `refactor` | 重构代码（不改变功能） |
| `docs` | 更新文档 |
| `test` | 添加/修改测试 |
| `chore` | 构建/工具/配置变更 |
| `hotfix` | 紧急修复 |

#### 示例

```
feat: pipeline 添加PPI算法执行器

- 实现PpiAlgoExecutor类
- 调用FastAPI执行PPI预测
- 添加单元测试

Closes #123
```

```
fix: auth 修复游客登录失败问题

- 修复GuestLoginStrategy中用户创建逻辑
- 添加游客登录的异常处理

Closes #124
```

```
refactor: predict 统一预测接口

- 废弃旧的predictDTI/PPI/DDI方法
- 统一使用predict通用方法

Closes #125
```

### 2.5 代码审查要求

| 审查项 | 要求 |
|:---|:---|
| 代码风格 | 符合项目规范，无未使用的import |
| 单元测试 | 核心逻辑必须有单元测试 |
| 错误处理 | 有完善的异常处理和日志 |
| 安全性 | 无SQL注入、XSS等安全问题 |
| 文档 | 新增接口有Swagger注解 |

### 2.6 版本标签规范

```bash
# 创建版本标签
git tag v1.0.0
git push origin v1.0.0

# 标签命名格式
v{主版本}.{次版本}.{修订版本}

# 示例
v1.0.0    # 第一个正式版本
v1.1.0    # 新增功能
v1.1.1    # 修复Bug
v2.0.0    # 重大重构
```

---

## 3. 分模块测试方案

### 3.1 测试分类

| 测试类型 | 说明 | 工具 |
|:---|:---|:---|
| 单元测试 | 测试单个类/方法 | JUnit5, pytest, Vitest |
| 集成测试 | 测试多个模块协作 | Spring Boot Test, Testcontainers |
| 接口测试 | 测试API接口 | MockMvc, httpx |
| 端到端测试 | 测试完整业务流程 | Playwright |

### 3.2 后端测试（Spring Boot）

#### 3.2.1 测试框架

| 工具 | 用途 |
|:---|:---|
| JUnit5 | 单元测试框架 |
| MockMvc | REST API测试 |
| Testcontainers | 容器化测试环境 |
| Mockito | Mock对象 |
| AssertJ | 流式断言 |

#### 3.2.2 测试命令

```bash
# 运行所有测试
mvn test

# 运行特定测试类
mvn test -Dtest=AuthServiceTest

# 运行特定测试方法
mvn test -Dtest=AuthServiceTest#testLogin_Success

# 运行特定包的测试
mvn test -Dtest=com.synpharm.service.*Test

# 跳过测试（仅编译）
mvn package -DskipTests

# 生成测试覆盖率报告（需要配置jacoco插件）
mvn test jacoco:report
# 报告路径: target/site/jacoco/index.html
```

#### 3.2.3 管道机制单元测试示例

**InputParser测试**：
```java
package com.synpharm.pipeline.impl;

import com.synpharm.enums.AlgoType;
import com.synpharm.enums.InputType;
import com.synpharm.pipeline.InputParser;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import static org.junit.jupiter.api.Assertions.*;

@SpringBootTest
class SmilesInputParserTest {

    @Autowired
    private InputParser smilesInputParser;

    @Test
    void testGetInputType() {
        assertEquals(InputType.SMILES, smilesInputParser.getInputType());
    }

    @Test
    void testParse_DTI() {
        String inputValue = "CC(=O)OC1=CC=CC=C1C(=O)O,MGLGLG...";
        var result = smilesInputParser.parse(inputValue, null, AlgoType.DTI);
        
        assertNotNull(result);
        assertEquals(2, result.getParams().size());
        assertEquals("CC(=O)OC1=CC=CC=C1C(=O)O", result.getParams().get(0));
    }

    @Test
    void testParse_InvalidInput() {
        assertThrows(IllegalArgumentException.class, () ->
                smilesInputParser.parse("", null, AlgoType.DTI));
    }
}
```

**AlgoExecutor测试**：
```java
package com.synpharm.pipeline.impl;

import com.synpharm.client.FastApiClient;
import com.synpharm.dto.response.AlgoResponse;
import com.synpharm.dto.response.BatchPredictionResponse;
import com.synpharm.enums.AlgoType;
import com.synpharm.pipeline.AlgoExecutor;
import com.synpharm.dto.ParsedInput;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;

import java.util.Arrays;
import java.util.List;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.Mockito.*;

@ExtendWith(MockitoExtension.class)
class DtiAlgoExecutorTest {

    @Mock
    private FastApiClient fastApiClient;

    @InjectMocks
    private DtiAlgoExecutor dtiAlgoExecutor;

    @Test
    void testGetAlgoType() {
        assertEquals(AlgoType.DTI, dtiAlgoExecutor.getAlgoType());
    }

    @Test
    void testExecute_Success() {
        // 准备数据
        ParsedInput input = new ParsedInput();
        input.setParams(Arrays.asList("smiles1", "seq1"));

        // Mock返回
        AlgoResponse mockResponse = new AlgoResponse();
        mockResponse.setStatus("success");
        when(fastApiClient.predictSingle(any())).thenReturn(mockResponse);

        // 执行
        AlgoResponse result = dtiAlgoExecutor.execute(input);

        // 验证
        assertNotNull(result);
        assertEquals("success", result.getStatus());
        verify(fastApiClient, times(1)).predictSingle(any());
    }

    @Test
    void testBatchExecute_Success() {
        // 准备数据
        List<ParsedInput> inputList = Arrays.asList(
                createParsedInput("smiles1", "seq1"),
                createParsedInput("smiles2", "seq2")
        );

        // Mock返回
        BatchPredictionResponse mockBatchResponse = new BatchPredictionResponse();
        mockBatchResponse.setStatus("success");
        mockBatchResponse.setTotal(2);
        mockBatchResponse.setResults(Arrays.asList(
                createAlgoResponse(),
                createAlgoResponse()
        ));
        when(fastApiClient.predictBatch(anyList(), anyString())).thenReturn(mockBatchResponse);

        // 执行
        List<AlgoResponse> results = dtiAlgoExecutor.batchExecute(inputList);

        // 验证
        assertNotNull(results);
        assertEquals(2, results.size());
        verify(fastApiClient, times(1)).predictBatch(anyList(), anyString());
    }

    private ParsedInput createParsedInput(String param1, String param2) {
        ParsedInput input = new ParsedInput();
        input.setParams(Arrays.asList(param1, param2));
        return input;
    }

    private AlgoResponse createAlgoResponse() {
        AlgoResponse response = new AlgoResponse();
        response.setStatus("success");
        return response;
    }
}
```

**PipelineFactory测试**：
```java
package com.synpharm.pipeline;

import com.synpharm.enums.AlgoType;
import com.synpharm.enums.InputType;
import com.synpharm.enums.OutputType;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import static org.junit.jupiter.api.Assertions.*;

@SpringBootTest
class DataPipelineFactoryTest {

    @Autowired
    private PipelineFactory pipelineFactory;

    @Test
    void testSupports_DTI_SMILES_JSON() {
        assertTrue(pipelineFactory.supports(InputType.SMILES, AlgoType.DTI, OutputType.JSON));
    }

    @Test
    void testSupports_PPI_SMILES_JSON() {
        assertTrue(pipelineFactory.supports(InputType.SMILES, AlgoType.PPI, OutputType.JSON));
    }

    @Test
    void testProcess_DTI_SMILES() {
        Object result = pipelineFactory.process(
                InputType.SMILES,
                AlgoType.DTI,
                OutputType.JSON,
                "CC(=O)OC1=CC=CC=C1C(=O)O,MGLGLG..."
        );
        assertNotNull(result);
    }

    @Test
    void testRegisteredInputParsers() {
        // 验证已注册的输入解析器数量
        // 已有: SMILES
        // 待开发: UNIPROT, PDB, CSV
    }

    @Test
    void testRegisteredAlgoExecutors() {
        // 验证已注册的算法执行器数量
        // 已有: DTI
        // 待开发: PPI, DDI
    }

    @Test
    void testRegisteredOutputFormatters() {
        // 验证已注册的输出格式化器数量
        // 已有: JSON
        // 待开发: CSV
    }
}
```

#### 3.2.4 AuthService单元测试示例

**AuthService单元测试**

```java
package com.synpharm.service;

import com.synpharm.dto.request.LoginRequest;
import com.synpharm.exception.BusinessException;
import com.synpharm.service.impl.AuthServiceImpl;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.Mockito.*;

@ExtendWith(MockitoExtension.class)
class AuthServiceTest {

    @Mock
    private LoginStrategyFactory loginStrategyFactory;

    @Mock
    private JwtUtils jwtUtils;

    @Mock
    private StringRedisTemplate redisTemplate;

    @InjectMocks
    private AuthServiceImpl authService;

    @Test
    void testLogin_Success() {
        LoginRequest request = new LoginRequest();
        request.setLoginType("qq_email");
        request.setEmail("test@qq.com");
        request.setCaptcha("123456");

        LoginResponse mockResponse = LoginResponse.builder()
                .accessToken("token")
                .isNewUser(false)
                .build();

        when(loginStrategyFactory.login(any(), any(), any()))
                .thenReturn(mockResponse);

        LoginResponse response = authService.login(request, null);
        assertNotNull(response);
        assertEquals("token", response.getAccessToken());
    }

    @Test
    void testLogin_Guest() {
        LoginRequest request = new LoginRequest();
        request.setLoginType("guest");

        LoginResponse mockResponse = LoginResponse.builder()
                .accessToken("token")
                .isNewUser(true)
                .build();

        when(loginStrategyFactory.login(any(), any(), any()))
                .thenReturn(mockResponse);

        LoginResponse response = authService.login(request, null);
        assertNotNull(response);
    }

    @Test
    void testResetPassword_CaptchaError() {
        when(captchaService.verifyCaptcha(any(), any(), any()))
                .thenReturn(false);

        assertThrows(BusinessException.class, () ->
                authService.resetPassword("test@qq.com", "123456", "newPassword"));
    }
}
```

**Controller接口测试**

```java
package com.synpharm.api;

import com.synpharm.dto.request.LoginRequest;
import com.synpharm.service.AuthService;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.MockMvc;

import static org.mockito.ArgumentMatchers.any;
import static org.mockito.Mockito.when;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.post;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.jsonPath;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

@WebMvcTest(AuthController.class)
class AuthControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @MockBean
    private AuthService authService;

    @Test
    void testLogin_QQEmail() throws Exception {
        mockMvc.perform(post("/api/auth/login")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content("{\"loginType\":\"qq_email\",\"email\":\"test@qq.com\",\"captcha\":\"123456\"}"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.code").value(200));
    }

    @Test
    void testLogin_Guest() throws Exception {
        mockMvc.perform(post("/api/auth/login")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content("{\"loginType\":\"guest\"}"))
                .andExpect(status().isOk());
    }

    @Test
    void testResetPassword() throws Exception {
        mockMvc.perform(post("/api/auth/password/reset")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content("{\"email\":\"test@qq.com\",\"captcha\":\"123456\",\"newPassword\":\"Test@123\"}"))
                .andExpect(status().isOk());
    }
}
```

### 3.3 FastAPI测试

#### 3.3.1 测试框架

| 工具 | 用途 |
|:---|:---|
| pytest | 测试框架 |
| httpx | HTTP客户端测试 |
| unittest.mock | Mock对象 |
| pytest-cov | 测试覆盖率 |

#### 3.3.2 测试命令

```bash
# 安装测试依赖
pip install pytest httpx pytest-cov

# 运行所有测试
pytest

# 运行特定测试文件
pytest tests/test_predict.py

# 运行特定测试函数
pytest tests/test_predict.py::test_predict_single_dti

# 生成测试覆盖率报告
pytest --cov=. --cov-report=html
# 报告路径: htmlcov/index.html

# 运行测试并显示详细输出
pytest -v
```

#### 3.3.3 路由匹配测试（现阶段重点）

```python
# 文件: tests/test_routing.py
"""
FastAPI路由匹配测试
验证三种算法类型（DTI/PPI/DDI）都能正确路由
"""
import pytest
from fastapi.testclient import TestClient
from main import app

client = TestClient(app)


class TestRouting:
    """路由匹配测试类"""

    def test_health_check(self):
        """测试健康检查接口"""
        response = client.get("/health")
        assert response.status_code == 200
        assert response.json()["status"] == "UP"

    def test_predict_single_dti_route(self):
        """测试DTI路由匹配"""
        response = client.post(
            "/v1/predict/single",
            json={
                "algo_type": "DTI",
                "drug_smiles": "CC(=O)OC1=CC=CC=C1C(=O)O",
                "target_seq": "MGLGLG"
            }
        )
        assert response.status_code == 200
        assert response.json()["status"] == "success"
        assert "metrics" in response.json()

    def test_predict_single_ppi_route(self):
        """测试PPI路由匹配"""
        response = client.post(
            "/v1/predict/single",
            json={
                "algo_type": "PPI",
                "protein_a": "MGLGLG",
                "protein_b": "MVHLTE"
            }
        )
        assert response.status_code == 200
        assert response.json()["status"] == "success"
        assert "metrics" in response.json()

    def test_predict_single_ddi_route(self):
        """测试DDI路由匹配"""
        response = client.post(
            "/v1/predict/single",
            json={
                "algo_type": "DDI",
                "drug_a": "CC(=O)OC1=CC=CC=C1C(=O)O",
                "drug_b": "c1ccccc1"
            }
        )
        assert response.status_code == 200
        assert response.json()["status"] == "success"
        assert "metrics" in response.json()

    def test_predict_single_unknown_algo_type(self):
        """测试未知算法类型返回错误"""
        response = client.post(
            "/v1/predict/single",
            json={
                "algo_type": "UNKNOWN",
                "drug_smiles": "CC"
            }
        )
        assert response.status_code == 400

    def test_predict_batch_dti_route(self):
        """测试批量DTI路由匹配"""
        response = client.post(
            "/v1/predict/batch",
            json={
                "algo_type": "DTI",
                "data_list": [
                    {"drug_smiles": "CC(=O)OC1=CC=CC=C1C(=O)O", "target_seq": "MGLGLG"},
                    {"drug_smiles": "CCO", "target_seq": "MVHLTE"}
                ]
            }
        )
        assert response.status_code == 200
        assert response.json()["total"] == 2

    def test_predict_batch_ppi_route(self):
        """测试批量PPI路由匹配"""
        response = client.post(
            "/v1/predict/batch",
            json={
                "algo_type": "PPI",
                "data_list": [
                    {"protein_a": "MGLGLG", "protein_b": "MVHLTE"},
                    {"protein_a": "ABCDEF", "protein_b": "XYZABC"}
                ]
            }
        )
        assert response.status_code == 200
        assert response.json()["total"] == 2

    def test_predict_batch_ddi_route(self):
        """测试批量DDI路由匹配"""
        response = client.post(
            "/v1/predict/batch",
            json={
                "algo_type": "DDI",
                "data_list": [
                    {"drug_a": "CC(=O)OC1=CC=CC=C1C(=O)O", "drug_b": "c1ccccc1"},
                    {"drug_a": "CCO", "drug_b": "CCN"}
                ]
            }
        )
        assert response.status_code == 200
        assert response.json()["total"] == 2

    def test_predict_batch_unknown_algo_type(self):
        """测试批量未知算法类型返回错误"""
        response = client.post(
            "/v1/predict/batch",
            json={
                "algo_type": "UNKNOWN",
                "data_list": [{"drug_smiles": "CC"}]
            }
        )
        assert response.status_code == 400

    def test_predict_single_missing_params_dti(self):
        """测试DTI缺少参数时的校验"""
        response = client.post(
            "/v1/predict/single",
            json={"algo_type": "DTI"}
        )
        # Pydantic会自动校验缺失字段，返回422
        assert response.status_code == 422
```

#### 3.3.4 算法服务单元测试示例

```python
# 文件: tests/test_dti_service.py
"""
DTI算法服务单元测试
"""
import pytest
from services.dti_service import DTIService


class TestDTIService:

    def setup_method(self):
        self.service = DTIService()

    def test_predict_returns_metrics(self):
        """测试predict方法返回正确结构"""
        data = {
            "drug_smiles": "CC(=O)OC1=CC=CC=C1C(=O)O",
            "target_seq": "MGLGLG"
        }
        result = self.service.predict(data)
        
        assert result is not None
        assert hasattr(result, 'confidence_score')
        assert hasattr(result, 'confidence_level')
        assert 0 <= result.confidence_score <= 1

    def test_predict_confidence_level_high(self):
        """测试高置信度返回high"""
        data = {
            "drug_smiles": "CC(=O)OC1=CC=CC=C1C(=O)O",
            "target_seq": "MGLGLG"
        }
        result = self.service.predict(data)
        
        assert result.confidence_level in ["high", "medium", "low"]

    def test_predict_interactions_not_empty(self):
        """测试相互作用列表不为空"""
        data = {
            "drug_smiles": "CC(=O)OC1=CC=CC=C1C(=O)O",
            "target_seq": "MGLGLG"
        }
        result = self.service.predict(data)
        
        assert result.interactions is not None
        assert len(result.interactions) > 0
```

### 3.4 前端测试

#### 3.4.1 测试框架

| 工具 | 用途 |
|:---|:---|
| Vitest | 单元测试框架（Vue3推荐） |
| Vue Test Utils | Vue组件测试 |
| Playwright | 端到端测试 |

#### 3.4.2 测试命令

```bash
# 安装测试依赖
npm install -D vitest @vue/test-utils

# 运行所有测试
npm run test

# 运行特定测试文件
npx vitest run src/stores/auth.test.ts

# 测试覆盖率
npx vitest run --coverage

# 端到端测试
npx playwright test
```

#### 3.4.3 测试示例

**Auth Store单元测试**

```typescript
// 文件: src/stores/auth.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { setActivePinia, createPinia } from 'pinia'
import { useAuthStore } from './auth'
import { authApi } from '@/api/auth'

vi.mock('@/api/auth', () => ({
  authApi: {
    login: vi.fn(),
    logout: vi.fn(),
    sendCaptcha: vi.fn(),
    resetPassword: vi.fn()
  }
}))

describe('Auth Store', () => {
  beforeEach(() => {
    setActivePinia(createPinia())
    vi.clearAllMocks()
  })

  it('should login successfully', async () => {
    const mockUser = { id: '1', email: 'test@qq.com', nickname: 'Test', createdAt: '2024-01-01' }
    ;(authApi.login as vi.Mock).mockResolvedValue({ token: 'token', user: mockUser })

    const store = useAuthStore()
    const result = await store.login({ loginType: 'qq_email', email: 'test@qq.com', captcha: '123456' })

    expect(result.success).toBe(true)
    expect(store.user).toEqual(mockUser)
    expect(store.token).toBe('token')
    expect(store.isLoggedIn).toBe(true)
  })

  it('should login as guest', async () => {
    const mockUser = { id: 'guest_1', email: 'guest@guest.local', nickname: '游客', createdAt: '2024-01-01' }
    ;(authApi.login as vi.Mock).mockResolvedValue({ token: 'guest_token', user: mockUser })

    const store = useAuthStore()
    const result = await store.login({ loginType: 'guest' })

    expect(result.success).toBe(true)
    expect(store.isGuest).toBe(true)
  })

  it('should logout successfully', async () => {
    const store = useAuthStore()
    store.user = { id: '1', email: 'test@qq.com', nickname: 'Test', createdAt: '2024-01-01' }
    store.token = 'token'
    store.isLoggedIn = true

    await store.logout()

    expect(store.user).toBeNull()
    expect(store.token).toBeNull()
    expect(store.isLoggedIn).toBe(false)
  })

  it('should send captcha', async () => {
    ;(authApi.sendCaptcha as vi.Mock).mockResolvedValue({ success: true })

    const store = useAuthStore()
    const result = await store.sendCaptcha('test@qq.com', 'login')

    expect(result.success).toBe(true)
    expect(authApi.sendCaptcha).toHaveBeenCalledWith('test@qq.com', 'login')
  })

  it('should reset password', async () => {
    ;(authApi.resetPassword as vi.Mock).mockResolvedValue()

    const store = useAuthStore()
    const result = await store.resetPassword('test@qq.com', '123456', 'newPassword')

    expect(result.success).toBe(true)
    expect(authApi.resetPassword).toHaveBeenCalledWith('test@qq.com', '123456', 'newPassword')
  })
})
```

**Login组件测试**

```typescript
// 文件: src/views/Login.test.ts
import { describe, it, expect, vi } from 'vitest'
import { mount } from '@vue/test-utils'
import Login from './Login.vue'
import { createRouter, createMemoryHistory } from 'vue-router'
import { createPinia, setActivePinia } from 'pinia'

const router = createRouter({
  history: createMemoryHistory(),
  routes: [{ path: '/dashboard', name: 'Dashboard' }]
})

describe('Login.vue', () => {
  beforeEach(() => {
    setActivePinia(createPinia())
  })

  it('should render login form', () => {
    const wrapper = mount(Login, {
      global: { plugins: [router] }
    })
    expect(wrapper.find('form').exists()).toBe(true)
    expect(wrapper.find('input[type="email"]').exists()).toBe(true)
    expect(wrapper.find('input[type="text"]').exists()).toBe(true)
  })

  it('should show error when email is invalid', async () => {
    const wrapper = mount(Login, {
      global: { plugins: [router] }
    })
    const emailInput = wrapper.find('input[type="email"]')
    await emailInput.setValue('invalid-email')
    await emailInput.trigger('blur')
    
    expect(wrapper.find('.login__form-error').text()).toBe('请输入正确的QQ邮箱（如12345678@qq.com）')
  })

  it('should show reset password modal', async () => {
    const wrapper = mount(Login, {
      global: { plugins: [router] }
    })
    await wrapper.find('.login__form-link').trigger('click')
    
    expect(wrapper.find('.login__modal').exists()).toBe(true)
  })

  it('should handle guest login', async () => {
    const wrapper = mount(Login, {
      global: { plugins: [router] }
    })
    const guestBtn = wrapper.find('.login__form-btn--outline')
    await guestBtn.trigger('click')
    
    expect(wrapper.vm.showResetModal).toBe(false)
  })
})
```

### 3.5 测试覆盖率要求

| 模块 | 覆盖率要求 |
|:---|:---|
| 后端Service层 | ≥80% |
| 后端Controller层 | ≥70% |
| FastAPI路由 | ≥70% |
| 前端Store | ≥80% |
| 前端组件 | ≥60% |

### 3.6 测试执行策略

```
┌─────────────────────────────────────────────────────────┐
│                    测试执行策略                         │
├─────────────────────────────────────────────────────────┤
│                                                        │
│  开发阶段                                              │
│  ├── 编写代码后立即编写单元测试                          │
│  └── 本地运行测试确保通过                               │
│                                                        │
│  提交代码前                                            │
│  ├── 运行全部单元测试                                   │
│  ├── 检查测试覆盖率                                     │
│  └── 确保无新增错误                                     │
│                                                        │
│  创建PR后                                              │
│  ├── CI自动运行测试                                     │
│  ├── 代码审查                                          │
│  └── 修复审查反馈                                       │
│                                                        │
│  合并到develop后                                        │
│  ├── 运行集成测试                                       │
│  └── 验证端到端流程                                     │
│                                                        │
│  发布到生产前                                          │
│  ├── 运行全部测试                                       │
│  ├── 人工验证关键流程                                    │
│  └── 创建版本标签                                       │
│                                                        │
└─────────────────────────────────────────────────────────┘
```

---

**版本**: v2.0.0  
**更新日期**: 2026-07-26  
**适用场景**: 团队开发规范、代码提交、测试验证
**更新内容**: 完善分支创建流程、部署环境步骤、管道机制测试示例、FastAPI路由匹配测试