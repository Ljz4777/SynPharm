# 组员任务清单

## 目录

1. [任务概述](#1-任务概述)
2. [通用流程](#2-通用流程)
3. [算法执行器开发（高优先级）](#3-算法执行器开发高优先级)
4. [输入解析器开发（中优先级）](#4-输入解析器开发中优先级)
5. [输出格式化器开发（中优先级）](#5-输出格式化器开发中优先级)
6. [数据库CRUD开发](#6-数据库crud开发)
7. [测试验证](#7-测试验证)
8. [Git提交规范](#8-git提交规范)

---

## 1. 任务概述

### 1.1 总体目标

完成三个算法类型（DTI/PPI/DDI）的完整数据流实现，支持多种输入类型（smiles/uniprot/pdb/csv）和输出类型（json/csv）。

### 1.2 任务分配原则

| 任务类型 | 说明 | 难度 | 重复性 |
|:---|:---|:---|:---|
| **算法执行器** | 创建 `AlgoExecutor` 实现类，调用FastAPI | 低 | 高 |
| **输入解析器** | 创建 `InputParser` 实现类，解析输入 | 中 | 中 |
| **输出格式化器** | 创建 `OutputFormatter` 实现类，格式化输出 | 低 | 高 |
| **数据库CRUD** | 创建Entity、Mapper、Service、Controller | 低 | 高 |

---

## 2. 通用流程

### 2.1 完整开发流程（每个任务都遵循此流程）

```
┌─────────────────────────────────────────────────────────┐
│                完整开发流程（6步走）                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  第1步：环境准备                                        │
│  ├── 阅读开发文档，理解需求                              │
│  ├── 确认本地开发环境已搭建                              │
│  └── 参考: deploy/部署指南.md                           │
│                                                         │
│  第2步：创建分支                                        │
│  ├── 从develop拉取最新代码                              │
│  ├── 按照命名规范创建功能分支                            │
│  └── 参考: development/开发规范与工程指南.md → 第2章   │
│                                                         │
│  第3步：编写代码                                        │
│  ├── 根据开发文档实现功能                                │
│  ├── 编写单元测试                                       │
│  └── 参考: modules/predict/下的开发文档                 │
│                                                         │
│  第4步：本地测试                                        │
│  ├── 运行单元测试                                       │
│  ├── 运行接口测试                                       │
│  └── 参考: development/开发规范与工程指南.md → 第3章   │
│                                                         │
│  第5步：提交代码                                        │
│  ├── 按照规范编写提交信息                                │
│  ├── 推送到远程仓库                                     │
│  └── 参考: development/开发规范与工程指南.md → 第2章   │
│                                                         │
│  第6步：创建PR                                          │
│  ├── 在GitHub创建Pull Request                           │
│  ├── 填写PR描述和测试说明                               │
│  └── 请求代码审查                                       │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 2.2 必备环境

| 环境 | 要求 | 验证命令 | 参考文档 |
|:---|:---|:---|:---|
| Java | JDK 21 | `java -version` | [部署指南.md](../deploy/部署指南.md) |
| Maven | 3.8+ | `mvn -version` | [部署指南.md](../deploy/部署指南.md) |
| MySQL | 8.0+ | `docker exec mysql-synpharm mysql -e "SELECT 1"` | [部署指南.md](../deploy/部署指南.md) |
| Redis | 7.0+ | `docker exec redis-synpharm redis-cli ping` | [部署指南.md](../deploy/部署指南.md) |
| Python | 3.10+（FastAPI） | `python --version` | [部署指南.md](../deploy/部署指南.md) |

### 2.3 快速开始（5分钟搭建开发环境）

**方式1：使用Docker一键启动（推荐）**

```bash
# 进入docker目录
cd d:\SynPharm\docker

# 启动所有服务
docker compose up -d --build

# 查看服务状态
docker compose ps
```

**方式2：手动启动**

```bash
# 1. 启动基础服务（MySQL + Redis，需先安装Docker）
docker run -d --name mysql-synpharm -p 3306:3306 -e MYSQL_ROOT_PASSWORD=root123 -e MYSQL_DATABASE=synpharm -e MYSQL_USER=synpharm -e MYSQL_PASSWORD=synpharm_pass mysql:8.0
docker run -d --name redis-synpharm -p 6379:6379 redis:7-alpine

# 2. 启动FastAPI
cd synpharm-fastapi
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 8000 --reload

# 3. 启动Spring Boot（新开终端）
cd synpharm-backend
mvn clean package -DskipTests
java -jar target/synpharm-backend-1.0.0.jar

# 4. 验证
curl http://localhost:8000/health
curl -X POST http://localhost:8080/api/auth/captcha/send -H "Content-Type: application/json" -d '{"email":"test@qq.com","type":"login"}'
```

详细步骤请参考 [部署指南.md](../deploy/部署指南.md)

### 2.4 各任务开发文档

| 任务类型 | 开发文档路径 |
|:---|:---|
| DTI算法执行器 | [DTI算法开发操作文档.md](../modules/predict/algorithm/DTI算法开发操作文档.md) |
| PPI算法执行器 | [PPI算法开发操作文档.md](../modules/predict/algorithm/PPI算法开发操作文档.md) |
| DDI算法执行器 | [DDI算法开发操作文档.md](../modules/predict/algorithm/DDI算法开发操作文档.md) |
| UniProt输入解析器 | [UniProt输入解析器开发操作文档.md](../modules/predict/data/UniProt输入解析器开发操作文档.md) |
| PDB输入解析器 | [PDB输入解析器开发操作文档.md](../modules/predict/data/PDB输入解析器开发操作文档.md) |
| CSV输入解析器 | [CSV输入解析器开发操作文档.md](../modules/predict/data/CSV输入解析器开发操作文档.md) |
| 数据流总览 | [数据流开发操作文档.md](../modules/predict/数据流开发操作文档.md) |

---

## 3. 算法执行器开发（高优先级）

### 3.1 任务列表

| 任务 | 文件路径 | 说明 | 负责人 | 状态 |
|:---|:---|:---|:---|:---|
| DTI算法执行器 | `pipeline/impl/DtiAlgoExecutor.java` | 调用FastAPI执行DTI预测 | 已完成 | ✅已完成 |
| PPI算法执行器 | `pipeline/impl/PpiAlgoExecutor.java` | 调用FastAPI执行PPI预测 | 组员A | ❌未开始 |
| DDI算法执行器 | `pipeline/impl/DdiAlgoExecutor.java` | 调用FastAPI执行DDI预测 | 组员B | ❌未开始 |

### 3.2 代码模板

```java
package com.synpharm.pipeline.impl;

import com.synpharm.client.FastApiClient;
import com.synpharm.dto.ParsedInput;
import com.synpharm.dto.request.PredictRequest;
import com.synpharm.dto.response.AlgoResponse;
import com.synpharm.dto.response.BatchPredictionResponse;
import com.synpharm.enums.AlgoType;
import com.synpharm.pipeline.AlgoExecutor;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Component;

import java.util.ArrayList;
import java.util.List;

@Slf4j
@Component
@RequiredArgsConstructor
public class {AlgoType}AlgoExecutor implements AlgoExecutor {

    private final FastApiClient fastApiClient;

    @Override
    public AlgoType getAlgoType() {
        return AlgoType.{ALGO_TYPE_ENUM};
    }

    @Override
    public AlgoResponse execute(ParsedInput inputData) {
        log.info("执行{}算法: params={}", getAlgoType().getCode(), inputData.getParams());
        
        List<String> params = inputData.getParams();
        PredictRequest request = PredictRequest.for{ALGO_TYPE}(params.get(0), params.get(1));
        
        AlgoResponse response = fastApiClient.predictSingle(request);
        response.setAlgoType(getAlgoType().getCode());
        
        return response;
    }

    @Override
    public List<AlgoResponse> batchExecute(List<ParsedInput> inputDataList) {
        log.info("批量执行{}算法: 数量={}", getAlgoType().getCode(), inputDataList.size());
        
        List<PredictRequest> requests = new ArrayList<>();
        for (ParsedInput input : inputDataList) {
            List<String> params = input.getParams();
            requests.add(PredictRequest.for{ALGO_TYPE}(params.get(0), params.get(1)));
        }
        
        BatchPredictionResponse batchResponse = fastApiClient.predictBatch(requests, getAlgoType().getCode());
        
        List<AlgoResponse> responses = new ArrayList<>();
        if (batchResponse != null && batchResponse.getResults() != null) {
            for (var result : batchResponse.getResults()) {
                AlgoResponse response = new AlgoResponse();
                response.setStatus("success");
                response.setAlgoType(getAlgoType().getCode());
                response.setMetrics(result.getMetrics());
                responses.add(response);
            }
        }
        
        return responses;
    }
}
```

### 3.3 DtiAlgoExecutor（参考示例 - 已完成）

```java
// 文件: pipeline/impl/DtiAlgoExecutor.java
@Slf4j
@Component
@RequiredArgsConstructor
public class DtiAlgoExecutor implements AlgoExecutor {

    private final FastApiClient fastApiClient;

    @Override
    public AlgoType getAlgoType() {
        return AlgoType.DTI;
    }

    @Override
    public AlgoResponse execute(ParsedInput inputData) {
        log.info("执行DTI算法: params={}", inputData.getParams());
        
        List<String> params = inputData.getParams();
        PredictRequest request = PredictRequest.forDTI(params.get(0), params.get(1));
        
        AlgoResponse response = fastApiClient.predictSingle(request);
        response.setAlgoType(AlgoType.DTI.getCode());
        
        return response;
    }

    @Override
    public List<AlgoResponse> batchExecute(List<ParsedInput> inputDataList) {
        log.info("批量执行DTI算法: 数量={}", inputDataList.size());
        
        List<PredictRequest> requests = new ArrayList<>();
        for (ParsedInput input : inputDataList) {
            List<String> params = input.getParams();
            requests.add(PredictRequest.forDTI(params.get(0), params.get(1)));
        }
        
        BatchPredictionResponse batchResponse = fastApiClient.predictBatch(requests, AlgoType.DTI.getCode());
        
        List<AlgoResponse> responses = new ArrayList<>();
        if (batchResponse != null && batchResponse.getResults() != null) {
            for (var result : batchResponse.getResults()) {
                AlgoResponse response = new AlgoResponse();
                response.setStatus("success");
                response.setAlgoType(AlgoType.DTI.getCode());
                response.setMetrics(result.getMetrics());
                responses.add(response);
            }
        }
        
        return responses;
    }
}
```

### 3.4 PpiAlgoExecutor 具体实现

```java
// 文件: pipeline/impl/PpiAlgoExecutor.java
@Slf4j
@Component
@RequiredArgsConstructor
public class PpiAlgoExecutor implements AlgoExecutor {

    private final FastApiClient fastApiClient;

    @Override
    public AlgoType getAlgoType() {
        return AlgoType.PPI;
    }

    @Override
    public AlgoResponse execute(ParsedInput inputData) {
        log.info("执行PPI算法: params={}", inputData.getParams());
        
        List<String> params = inputData.getParams();
        PredictRequest request = PredictRequest.forPPI(params.get(0), params.get(1));
        
        AlgoResponse response = fastApiClient.predictSingle(request);
        response.setAlgoType(AlgoType.PPI.getCode());
        
        return response;
    }

    @Override
    public List<AlgoResponse> batchExecute(List<ParsedInput> inputDataList) {
        log.info("批量执行PPI算法: 数量={}", inputDataList.size());
        
        List<PredictRequest> requests = new ArrayList<>();
        for (ParsedInput input : inputDataList) {
            List<String> params = input.getParams();
            requests.add(PredictRequest.forPPI(params.get(0), params.get(1)));
        }
        
        BatchPredictionResponse batchResponse = fastApiClient.predictBatch(requests, AlgoType.PPI.getCode());
        
        List<AlgoResponse> responses = new ArrayList<>();
        if (batchResponse != null && batchResponse.getResults() != null) {
            for (var result : batchResponse.getResults()) {
                AlgoResponse response = new AlgoResponse();
                response.setStatus("success");
                response.setAlgoType(AlgoType.PPI.getCode());
                response.setMetrics(result.getMetrics());
                responses.add(response);
            }
        }
        
        return responses;
    }
}
```

### 3.5 DdiAlgoExecutor 具体实现

```java
// 文件: pipeline/impl/DdiAlgoExecutor.java
@Slf4j
@Component
@RequiredArgsConstructor
public class DdiAlgoExecutor implements AlgoExecutor {

    private final FastApiClient fastApiClient;

    @Override
    public AlgoType getAlgoType() {
        return AlgoType.DDI;
    }

    @Override
    public AlgoResponse execute(ParsedInput inputData) {
        log.info("执行DDI算法: params={}", inputData.getParams());
        
        List<String> params = inputData.getParams();
        PredictRequest request = PredictRequest.forDDI(params.get(0), params.get(1));
        
        AlgoResponse response = fastApiClient.predictSingle(request);
        response.setAlgoType(AlgoType.DDI.getCode());
        
        return response;
    }

    @Override
    public List<AlgoResponse> batchExecute(List<ParsedInput> inputDataList) {
        log.info("批量执行DDI算法: 数量={}", inputDataList.size());
        
        List<PredictRequest> requests = new ArrayList<>();
        for (ParsedInput input : inputDataList) {
            List<String> params = input.getParams();
            requests.add(PredictRequest.forDDI(params.get(0), params.get(1)));
        }
        
        BatchPredictionResponse batchResponse = fastApiClient.predictBatch(requests, AlgoType.DDI.getCode());
        
        List<AlgoResponse> responses = new ArrayList<>();
        if (batchResponse != null && batchResponse.getResults() != null) {
            for (var result : batchResponse.getResults()) {
                AlgoResponse response = new AlgoResponse();
                response.setStatus("success");
                response.setAlgoType(AlgoType.DDI.getCode());
                response.setMetrics(result.getMetrics());
                responses.add(response);
            }
        }
        
        return responses;
    }
}
```

---

## 4. 输入解析器开发（中优先级）

### 4.1 任务列表

| 任务 | 文件路径 | 说明 | 负责人 |
|:---|:---|:---|:---|
| UniProt输入解析器 | `pipeline/impl/UniprotInputParser.java` | 解析UniProt ID，查询蛋白质序列 | 组员A |
| PDB输入解析器 | `pipeline/impl/PdbInputParser.java` | 解析PDB文件路径，读取蛋白质结构 | 组员B |
| CSV输入解析器 | `pipeline/impl/CsvInputParser.java` | 解析CSV文件，支持批量预测 | 组员C |

### 4.2 代码模板

```java
package com.synpharm.pipeline.impl;

import com.synpharm.dto.request.PredictRequest;
import com.synpharm.enums.AlgoType;
import com.synpharm.enums.InputType;
import com.synpharm.pipeline.InputParser;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Component;

@Slf4j
@Component
public class {InputType}InputParser implements InputParser<PredictRequest> {

    @Override
    public InputType getInputType() {
        return InputType.{INPUT_TYPE_ENUM};
    }

    @Override
    public PredictRequest parse(String inputValue, String fileUrl, AlgoType algoType) {
        log.debug("解析{}输入: {}, algoType={}", getInputType().getCode(), inputValue, algoType.getCode());
        
        return switch (algoType) {
            case DTI -> PredictRequest.forDTI(param1, param2);
            case PPI -> PredictRequest.forPPI(param1, param2);
            case DDI -> PredictRequest.forDDI(param1, param2);
        };
    }
}
```

### 4.3 UniprotInputParser 具体实现

```java
// 文件: pipeline/impl/UniprotInputParser.java
@Component
public class UniprotInputParser implements InputParser<PredictRequest> {
    
    // TODO: 注入蛋白质序列查询服务（后续实现）
    
    @Override
    public InputType getInputType() {
        return InputType.UNIPROT;
    }
    
    @Override
    public PredictRequest parse(String inputValue, String fileUrl, AlgoType algoType) {
        log.debug("解析UniProt输入: {}, algoType={}", inputValue, algoType.getCode());
        
        if (inputValue == null || inputValue.trim().isEmpty()) {
            throw new IllegalArgumentException("UniProt输入不能为空");
        }
        
        String[] parts = inputValue.split(",");
        if (parts.length < 2) {
            throw new IllegalArgumentException("UniProt输入格式错误，需要逗号分隔的两个参数");
        }
        
        String param1 = parts[0].trim();
        String param2 = parts[1].trim();
        
        // TODO: 调用UniProt API查询蛋白质序列
        // String sequence1 = uniprotService.getSequence(param1);
        // String sequence2 = uniprotService.getSequence(param2);
        
        return switch (algoType) {
            case DTI -> PredictRequest.forDTI(param1, param2);
            case PPI -> PredictRequest.forPPI(param1, param2);
            case DDI -> PredictRequest.forDDI(param1, param2);
        };
    }
}
```

---

## 5. 输出格式化器开发（中优先级）

### 5.1 任务列表

| 任务 | 文件路径 | 说明 | 负责人 |
|:---|:---|:---|:---|
| CSV输出格式化器 | `pipeline/impl/CsvOutputFormatter.java` | 将预测结果格式化为CSV格式 | 组员C |

### 5.2 代码模板

```java
package com.synpharm.pipeline.impl;

import com.synpharm.dto.response.AlgoResponse;
import com.synpharm.dto.response.PredictResultResponse;
import com.synpharm.enums.OutputType;
import com.synpharm.pipeline.OutputFormatter;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Component;

import java.util.ArrayList;
import java.util.List;

@Slf4j
@Component
public class {OutputType}OutputFormatter implements OutputFormatter<AlgoResponse, PredictResultResponse> {

    @Override
    public OutputType getOutputType() {
        return OutputType.{OUTPUT_TYPE_ENUM};
    }

    @Override
    public PredictResultResponse format(AlgoResponse response) {
        if (response == null || response.getMetrics() == null) {
            throw new RuntimeException("预测结果为空");
        }
        
        log.debug("格式化{}输出: status={}, algoType={}", 
                getOutputType().getCode(), response.getStatus(), response.getAlgoType());
        
        var metrics = response.getMetrics();
        List<PredictResultResponse.InteractionInfo> interactions = new ArrayList<>();
        
        if (metrics.getInteractions() != null) {
            for (var interaction : metrics.getInteractions()) {
                interactions.add(PredictResultResponse.InteractionInfo.builder()
                        .residue(interaction.getResidue())
                        .type(interaction.getType())
                        .distance(interaction.getDistance())
                        .build());
            }
        }
        
        return PredictResultResponse.builder()
                .algoType(response.getAlgoType())
                .targetId(metrics.getTargetId())
                .targetName(metrics.getTargetName())
                .bindingAffinity(metrics.getBindingAffinity())
                .confidenceScore(metrics.getConfidenceScore())
                .confidenceLevel(metrics.getConfidenceLevel())
                .interactions(interactions)
                .build();
    }
}
```

---

## 6. 数据库CRUD开发

### 6.1 已有的表结构

| 表名 | 说明 | 状态 |
|:---|:---|:---|
| `sys_user` | 用户表 | ✅已创建 |
| `sys_login_log` | 登录日志表 | ✅已创建 |
| `predict_task` | 预测任务表 | ✅已创建 |
| `predict_result` | 预测结果表 | ✅已创建 |
| `user_favorite` | 用户收藏表 | ✅已创建 |
| `batch_task` | 批次任务表 | ✅已创建 |

### 6.2 需要开发的CRUD

| 模块 | 功能 | 接口 | 文件 |
|:---|:---|:---|:---|
| **用户管理** | 查询用户列表 | `GET /api/user/list` | `api/UserController.java` |
| | 查询用户详情 | `GET /api/user/{id}` | |
| | 更新用户信息 | `PUT /api/user/{id}` | |
| | 删除用户 | `DELETE /api/user/{id}` | |
| **任务管理** | 创建任务 | `POST /api/task` | `api/TaskController.java` |
| | 查询任务列表 | `GET /api/task/list` | |
| | 查询任务详情 | `GET /api/task/{id}` | |
| | 删除任务 | `DELETE /api/task/{id}` | |
| **结果管理** | 查询结果列表 | `GET /api/result/list` | `api/ResultController.java` |
| | 查询结果详情 | `GET /api/result/{id}` | |
| | 删除结果 | `DELETE /api/result/{id}` | |
| **收藏管理** | 添加收藏 | `POST /api/favorite` | `api/FavoriteController.java` |
| | 查询收藏列表 | `GET /api/favorite/list` | |
| | 删除收藏 | `DELETE /api/favorite/{id}` | |

### 6.3 CRUD开发步骤

#### 步骤1：创建Entity（已完成，无需修改）

```java
// 文件: model/entity/PredictResult.java
// 已有，直接使用
```

#### 步骤2：创建Mapper

```java
// 文件: repository/mapper/PredictResultMapper.java
public interface PredictResultMapper extends BaseMapper<PredictResult> {
    // MyBatis Plus自动提供CRUD方法
}
```

#### 步骤3：创建Service接口

```java
// 文件: service/PredictResultService.java
public interface PredictResultService extends IService<PredictResult> {
    List<PredictResult> getByUserId(Long userId);
    PredictResult getById(Long id);
    void deleteById(Long id);
}
```

#### 步骤4：创建Service实现

```java
// 文件: service/impl/PredictResultServiceImpl.java
@Service
@RequiredArgsConstructor
public class PredictResultServiceImpl extends ServiceImpl<PredictResultMapper, PredictResult> 
        implements PredictResultService {
    
    @Override
    public List<PredictResult> getByUserId(Long userId) {
        return lambdaQuery().eq(PredictResult::getUserId, userId).list();
    }
    
    @Override
    public PredictResult getById(Long id) {
        return getById(id);
    }
    
    @Override
    public void deleteById(Long id) {
        removeById(id);
    }
}
```

#### 步骤5：创建Controller

```java
// 文件: api/ResultController.java
@RestController
@RequestMapping("/api/result")
@RequiredArgsConstructor
public class ResultController {
    
    private final PredictResultService predictResultService;
    
    @GetMapping("/list")
    public Result<List<PredictResult>> list(@RequestParam Long userId) {
        return Result.success(predictResultService.getByUserId(userId));
    }
    
    @GetMapping("/{id}")
    public Result<PredictResult> getById(@PathVariable Long id) {
        return Result.success(predictResultService.getById(id));
    }
    
    @DeleteMapping("/{id}")
    public Result<Void> deleteById(@PathVariable Long id) {
        predictResultService.deleteById(id);
        return Result.success();
    }
}
```

---

## 7. 测试验证

### 7.1 测试检查清单（每个任务完成后必须验证）

| 检查项 | 说明 | 验证方法 |
|:---|:---|:---|
| **单元测试通过** | 核心逻辑有单元测试 | `mvn test -Dtest=类名` |
| **编译通过** | 项目能正常编译打包 | `mvn clean package -DskipTests` |
| **服务启动正常** | 应用能正常启动无报错 | 查看启动日志 |
| **接口可调用** | 接口返回200 | Postman/curl测试 |
| **功能正确** | 业务逻辑符合预期 | 对比输入输出 |
| **异常处理** | 异常情况有正确处理 | 传入错误参数测试 |
| **日志输出** | 有完善的日志记录 | 查看日志文件 |

### 7.2 Spring Boot管道组件测试

```bash
# 运行所有管道相关测试
mvn test -Dtest=*InputParserTest
mvn test -Dtest=*AlgoExecutorTest
mvn test -Dtest=*OutputFormatterTest
mvn test -Dtest=*PipelineFactoryTest

# 运行单个测试类
mvn test -Dtest=DtiAlgoExecutorTest
mvn test -Dtest=SmilesInputParserTest
```

### 7.3 FastAPI路由匹配测试

```bash
# 进入FastAPI目录
cd synpharm-fastapi
.\venv\Scripts\Activate.ps1

# 运行路由匹配测试
pytest tests/test_routing.py -v

# 运行所有测试
pytest -v
```

### 7.4 接口测试示例（curl）

**测试PPI算法执行器**：
```bash
# 1. 先登录获取token（游客模式）
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"loginType":"guest"}'

# 2. 调用预测接口（替换token）
curl -X POST http://localhost:8080/api/predict/general \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer {your-token}" \
  -d '{"inputType":"smiles","algoType":"PPI","outputType":"json","inputValue":"MGLGLG,MVHLTE"}'
```

**测试DDI算法执行器**：
```bash
curl -X POST http://localhost:8080/api/predict/general \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer {your-token}" \
  -d '{"inputType":"smiles","algoType":"DDI","outputType":"json","inputValue":"CC(=O)OC1=CC=CC=C1C(=O)O,c1ccccc1"}'
```

### 7.5 单元测试模板

**InputParser测试模板**：
```java
@SpringBootTest
class {InputType}InputParserTest {

    @Autowired
    private InputParser {inputType}InputParser;

    @Test
    void testGetInputType() {
        assertEquals(InputType.{INPUT_TYPE}, {inputType}InputParser.getInputType());
    }

    @Test
    void testParse_Success() {
        // 测试正常解析
    }

    @Test
    void testParse_InvalidInput() {
        // 测试异常输入
        assertThrows(IllegalArgumentException.class, () ->
                {inputType}InputParser.parse("", null, AlgoType.DTI));
    }
}
```

**AlgoExecutor测试模板**：
```java
@ExtendWith(MockitoExtension.class)
class {AlgoType}AlgoExecutorTest {

    @Mock
    private FastApiClient fastApiClient;

    @InjectMocks
    private {AlgoType}AlgoExecutor {algoType}AlgoExecutor;

    @Test
    void testGetAlgoType() {
        assertEquals(AlgoType.{ALGO_TYPE}, {algoType}AlgoExecutor.getAlgoType());
    }

    @Test
    void testExecute_Success() {
        // 测试正常执行
    }

    @Test
    void testBatchExecute_Success() {
        // 测试批量执行
    }
}
```

### 7.6 验证清单

| 验证项 | 预期结果 | 完成情况 |
|:---|:---|:---|
| 启动应用 | 控制台显示组件注册日志 | ☐ |
| 调用新组件接口 | 返回200 OK | ☐ |
| 单元测试通过 | 全部测试通过 | ☐ |
| 批量接口 | 返回正确的数量 | ☐ |
| 异常场景 | 返回正确的错误信息 | ☐ |
| 日志输出 | 有完整的info/error日志 | ☐ |

---

## 8. Git提交规范

### 8.1 分支命名

```
feature/{模块}-{功能}
例如:
- feature/ppi-algo-executor
- feature/uniprot-input-parser
- feature/csv-output-formatter
- feature/user-crud
- feature/task-crud
```

### 8.2 提交信息

```
{类型}: {模块} {功能描述}

类型:
- feat: 新功能
- fix: 修复bug
- refactor: 重构代码
- docs: 更新文档
- test: 添加测试

示例:
feat: pipeline 添加PPI算法执行器
feat: pipeline 添加UniProt输入解析器
feat: result 添加结果列表查询接口
```

### 8.3 提交步骤

```bash
# 1. 创建分支
git checkout -b feature/ppi-algo-executor

# 2. 开发代码
# ...编写代码...

# 3. 暂存更改
git add .

# 4. 提交代码
git commit -m "feat: pipeline 添加PPI算法执行器"

# 5. 推送到远程
git push origin feature/ppi-algo-executor

# 6. 创建合并请求
# 在GitHub上创建PR，选择main分支作为目标
```

---

## 附录：任务进度表

| 任务 | 状态 | 负责人 | 截止日期 |
|:---|:---|:---|:---|
| DtiAlgoExecutor | ✅已完成 | 已完成 | - |
| PpiAlgoExecutor | ❌未开始 | 组员A | 7月28日 |
| DdiAlgoExecutor | ❌未开始 | 组员B | 7月28日 |
| UniprotInputParser | ❌未开始 | 组员A | 7月30日 |
| PdbInputParser | ❌未开始 | 组员B | 7月30日 |
| CsvInputParser | ❌未开始 | 组员C | 8月1日 |
| CsvOutputFormatter | ❌未开始 | 组员C | 8月1日 |
| 用户管理CRUD | ❌未开始 | 组员A | 8月2日 |
| 任务管理CRUD | ❌未开始 | 组员B | 8月2日 |
| 结果管理CRUD | ❌未开始 | 组员C | 8月2日 |
| 收藏管理CRUD | ❌未开始 | 组员A | 8月3日 |

---

**版本**: v2.0.0  
**更新日期**: 2026-07-26  
**适用场景**: 组员任务分配与开发指导
**更新内容**: 完善6步开发流程，添加快速开始指南，整合分支/部署/测试流程