# SynPharm 未修复功能与问题清单

> 用途：一页看清"还剩什么没做"，并区分**能立刻做**与**必须等外部数据**。
> 详细条目与代码证据见 `SynPharm问题总账与整改方案（整合版）.md`。
> 最后更新：2026-09-23（本轮补齐干实验设计工作台之后）

---

## 0. 总览

| 项 | 数量 |
|---|---|
| 问题总账条目 | **119 条**（P0 15 / P1 41 / P2 46 / P3 18，含新增 A-09） |
| 已修复 | **18 条** |
| **剩余** | **约 101 条**（另新增一组「设计工作台待后端」项，属新功能而非缺陷） |

**按"是否需要外部数据"分：**

| 类别 | 数量 | 说明 |
|---|---|---|
| 等外部测试数据 | ~6 条 | 靶点库相关 |
| **不需要数据，随时可做** | **~96 条** | 代码 / 配置 / 文档 |

---

## 1. 已修复（对照基线，不再重复）

### 计算引擎与依赖
| ID | 内容 |
|---|---|
| — | torch 2.1.0→2.5.1、numpy→2.x、transformers 加上限、补回 rdkit（PPI 从不可用变可用） |
| — | 移除 numpy 别名兼容补丁（numpy 2 下已成空操作） |
| `F-07` | 依赖版本不可复现 |
| `B-20` | `torch.load` 未显式 `weights_only` |
| `C-03` | KAN-MoDTI 崩溃时静默返回假结果 `[0],[0],[0.5]` |
| `E-01` | `/health` 恒返回 healthy → 现在报告各算法真实就绪状态（不触发加载） |
| `E-02` | 模型加载失败永久缓存 `None` → 改为冷却 300 秒后可重试（补权重无需重启容器） |
| `E-03` | 404 被吞成 500 → 已分类的错误原样透出 |
| `E-06` | `async` 端点里做阻塞推理 → 改为同步 `def`，交给线程池 |

### 批量链路
| ID | 内容 |
|---|---|
| `A-01` | RabbitMQ 监听被注释 → 恢复，批量不再永久 PENDING |
| `A-05` | 结果 CSV 字段错位 → 对齐 snake_case 并透传输入列 |
| `A-06` | 批量"全失败"上报成功 → 引擎逐条统计 + 前端不再无条件标成功 |
| `A-09` | `ROW_NUMBER` 保留字未转义 → 实体加反引号 |
| `I-09` | CHANGELOG 声称批量 SUCCESS 但已回归 → 现已实测跑通 |

### DDI
| ID | 内容 |
|---|---|
| `A-02` | FlashPPI 权重缺失 + 依赖错位 |
| `A-03` | DDI 契约三方互斥 → 新增 `DdiDrugResolver` 转换层 + 白名单表 + 查询接口 |

### 前端
| ID | 内容 |
|---|---|
| `A-04` | 仍调已废弃的 `/dti` `/ppi` `/ddi` → 改走 `/api/predict/general` |
| `D-04` | 整体丢弃非 2xx 响应体 → 现在透出后端中文原因 + `errorCode` + HTTP 状态码 |
| `G-01` | "保存结果"按钮无绑定 → 接入 `POST /api/favorites`，带保存中/已保存态 |
| `G-02` | "高级选项"两个控件从不生效 → 阈值提示 + 详细结果开关真正生效（并标注为前端判定） |
| `G-06` | 结果面板串到批量/历史页签 → 限定 `mode === 'single'` |
| `G-10` | 批量轮询失败静默中止 → 现在给出明确错误 |

### 部署 / 配置
| ID | 内容 |
|---|---|
| `F-08`（部分） | `.env` 的 `VITE_API_BASE_URL` 由 `8080` 改为与 `deploy/.env` 的 `BACKEND_PORT` 一致的 `7000`，`.env.example` 补上「两处必须对齐」的说明。**未彻底解决**：`.env` 被 gitignore，构建产物的后端地址仍需部署时注入 |
| — | **前端镜像不构建分子编辑器子应用** → 已修：`Dockerfile` 新增 `RUN npm run build:ketcher`，`.dockerignore` 排除本地残留的 `public/ketcher/`。此前能跑只是因为开发者本地恰好有旧产物被 `COPY . .` 带入，**CI / 新克隆机器上编辑器必顶白屏** |

### 干实验设计工作台（新增能力，非缺陷修复）
| 项 | 内容 |
|---|---|
| — | 新增 `/design` 三栏工作台（画布 / 候选 / 对比 / 口袋四个页签），**纯增量接入**，既有页面零改动 |
| — | 2D 分子编辑器以**独立子应用 + iframe** 接入，约 29 MB 化学引擎不进主依赖图 |
| — | 候选分子 InChIKey / 分子量 / 分子式改由本地 Indigo **实算**回填（引擎未就绪则自动降级） |
| — | 3D 口袋视图新增腔体线框球与共结晶配体（对 `MolstarViewer` 为可选 prop 的纯增量扩展） |
| — | 修正演示数据取代基**反向接入**缺陷（标签 `COOH` 曾实为甲酸酯） |

---

## 2. 未修复 —— 按主题

### 2.1 安全 🔴 最紧急 · 8 条 · **不需要数据**

| ID | 问题 | 影响 |
|---|---|---|
| `B-01` | **MySQL root 口令 + QQ 邮箱授权码硬编码进仓库**（`application-dev.yml:9`、`application.yml:25`、`application-dev.yml:25`） | ⚠️ 凭据已在 git 历史中。**改代码不够，必须轮换密钥**；如仓库曾对外可见，应按已泄露处理 |
| `B-02` | 弱口令管理员随部署自动创建（`sql/07_test_data.sql:8`、`docker-compose.yml:31`） | 生产环境直接留后门 |
| `B-03` | 任务详情/取消**无归属校验**（`TaskController:54,68`） | 可查看、取消他人的任务 |
| `B-04` | JWT **只验签不验人**（`JwtAuthenticationFilter:71-95`） | 禁用/注销/改密后旧 token 仍有效 |
| `B-06` | 密码/验证码走 **URL query 明文传输**（`api/auth.ts:90-99,121-124`） | 泄露到日志、代理、浏览器历史 |
| `B-07` | CORS 任意源 + `allowCredentials(true)`（`CorsConfig:37,46`） | 任意站点可带凭据发起跨站请求 |
| `B-10` | FastAPI 生产默认**无认证** + `/docs` 裸奔 + 无长度限制 | 引擎可被任意调用 |
| `B-12` | fail-open 归属校验（`userId == null` 直接放行） | 越权读取收藏/批次 |

### 2.2 部署 / 配置 · 4 条 · **不需要数据**

| ID | 问题 | 影响 |
|---|---|---|
| `A-07` | **nginx 无 `client_max_body_size`**（默认 1MB），后端却允许 100MB | **一行配置，但不改就等于批量上传有 1MB 上限** |
| `F-04` | `sql/01`~`06` 全是 `DROP TABLE IF EXISTS` | 手工执行会**清库** |
| `F-06` | 前端 Dockerfile 跳过 `vue-tsc`；仓库无 CI；后端镜像无 JVM 内存参数 | 类型错误直接进镜像 |
| `I-01` | `上线流程详细版.md` §1.4 仍写"模型缺失静默返回假数据"（已过期） | 文档误导 |

### 2.3 前端 · 约 20 条 · **不需要数据**

| ID | 问题 |
|---|---|
| `D-02` | `inputType` 无切换控件 —— 首页宣传的多格式输入实际不存在 |
| `D-03` | 分页信封三套并存（`{total,list}` / `{total,page,pageSize,list}`） |
| `D-06` | `Predict.vue:632` 用 `as unknown as` 绕过类型检查 |
| `D-07` | 任务状态文案两套（`tk__status--` 拼接 vs `StatusTag`） |
| `F-09` | 无 404 兜底路由；`vite.config.ts` 无 dev 代理 |
| `G-03` | `rememberMe` 勾选框无任何行为 |
| `G-04` | 任务页"暂停"按钮空实现（后端也无该能力） |
| `G-05` | Dashboard 结果卡"删除"无绑定（同组件在 Results 正常） |
| `G-07` | 登出竞态：被守卫弹回 dashboard 再被 401 踢回登录页 |
| `G-08` | `getProfile()` 返回类型与实际不符，绕过 `normalizeUser` |
| `G-09` | 校验规则双轨且互相矛盾（PDB/UniProt 规则不同） |
| `G-11` | 切换算法类型不清空输入、不重校验 |
| `G-12` | `confidenceLevel` 筛选大小写硬比 |
| `G-13` | 时间格式化无非法值兜底 → `Invalid Date` |
| `G-14` | 多处 `v-for` 用下标作 key |
| `G-15` | 生产代码残留 20+ 处 `console.log` |
| `G-16` | 无自动刷新，预测完成后仪表盘/任务页数字不更新 |
| `G-17` | 无障碍缺失（modal 无 `role`/Esc/焦点陷阱） |
| `G-18` | 巨型组件（`Predict.vue` 2400 行、`MolstarViewer.vue` 1500 行）+ 约 880 行死 CSS |
| `G-19` | 游客账号无法自助删除（强制要求密码） |
| `G-20` | 3D 页文案声称"已使用兜底数据"，实现中无兜底 |
| `C-01` | 首页"快速预测演示"纯前端伪造（`setTimeout` + `Math.random()`） |
| `C-02` | 首页营销数字硬编码且与预测中心互斥 |

### 2.4 后端 / 业务 · 约 15 条

| ID | 问题 | 备注 |
|---|---|---|
| `C-05` | `PredictUtils` 用 `Random` 造假 | ⚠️ **已缓解未清除**：`predict()` 现已直接抛错，假数据不可达；但 `PredictUtils` 与 `TaskServiceImpl#executeTask` 仍是死代码（无 Controller 调用），待专门清理 |
| `C-06` | `executeTask` 必然失败 + 失败状态被事务回滚 | 同上，随 `executeTask` 一起清理 |
| `C-07` | 单条预测无指纹幂等 + 保存失败静默（重复记录） |
| `D-01` | `DDIPredictRequest.drugASmiles` 命名与语义不符 |
| `D-05` | 单条预测在事务内做 30~60s 外部 HTTP |
| `E-05` | 错误日志只记 `getMessage()`，丢堆栈 |
| `E-07` | `/api/system/algorithm-health` 的值现在可信了（依赖 `E-01` 已修），但接口本身未复测 |
| `E-08` | Redis 降级静默（仅 `log.warn`），无指标无告警 |
| `B-05` | 登出写黑名单失败只 `log.warn`，仍返回成功 |
| `B-08` | 账户锁定可被恶意触发（仅按 account）+ 账号枚举 |
| `B-09` | 游客登录无验证码/无限流，可无限建号 |
| `B-11` | `.env.example` Key 格式（JSON 数组）与解析（逗号）不符 |
| `B-13` | 生产 SQL 全量参数日志 + debug + actuator `always` |
| `B-15` | 验证码无 60s 冷却、无 IP 限流、计数永不过期 |
| `B-16` | 验证码 Redis key 未归一化邮箱 → **大写 QQ 邮箱必然失败** |
| `B-17` | 修改密码无强度校验（可设为 `123`） |
| `B-18` | 邮件发送失败被吞，用户以为已发送 |

### 2.5 算法引擎 · 约 8 条

| ID | 问题 | 备注 |
|---|---|---|
| `C-04` | DTI 靶点写死 `P00533/EGFR`，`binding_affinity` 与 `interactions` 恒空 | 三个 service 各一份重复实现 |
| `C-10` | 结果字段语义不实（`confidence_level` 套在"相互作用概率"上） |
| `E-04` | 错误响应无 `algo_type` 字段 | 原因码部分已补（新增 `OutOfGraphError`/`InferenceError`） |
| `F-03` | `DEVICE`/`MODEL_DIR`/`BATCH_SIZE` 配置 | `MODEL_DIR`/`DEVICE` 已改为真实读取，`BATCH_SIZE` 仍无效 |
| `A-08` | 批量失败原因不可见（前端未接 `/api/batch/{id}/items`） |
| — | DDI 输出占位字段（`target_id` 已改为药物对；`binding_affinity`/`interactions` 对 DDI 本就无意义） |

### 2.6 数据库 · 4 条

| ID | 问题 |
|---|---|
| `F-05` | 无外键约束完整覆盖；`predict_result` 无内容唯一约束；`create_time/update_time` 未纳入自动填充 |
| `F-02` | `application-docker.yml` 把 `api-key` 错放在 `logging` 节点下 |
| `F-01` | 三个 profile 均开 `StdOutImpl` |
| — | `sql/09_*` 编号冲突（两个文件同前缀），且 `09_sys_user_research_profile.sql` **不幂等** |

### 2.7 等外部测试数据 · 约 6 条

详见 `测试数据准备清单.md`。

| ID | 问题 | 阻塞原因 |
|---|---|---|
| `C-08` | 靶点库整体 mock，后端无 `/api/targets`、无 targets 表 | **需要靶点数据**（UniProt ID + 名称 + 基因名 + 序列） |
| `SRC-03` | 测试数据太少（`sql/` 下无 `seed/`） | 需定种子覆盖场景 |
| `SRC-04` / `C-09` | 3D 结构无来源标注、不渲染配体、打分 fallback 不可靠 | 部分需要 PDB 结构数据 |
| `SRC-07` | 靶点多级分类（前端 `Set` 去重） | 需要靶点分类字段 |
| `SRC-08` | 3D 需更多入口 | 依赖 `C-09` |

### 2.8 测试与文档 · 3 条

| ID | 问题 |
|---|---|
| `H-05` | **测试全为 Mockito 单测，无 `@SpringBootTest` 集成测试，FastAPI 侧零测试** |
| — | 首页/靶点页的 `mockResults.ts` 仍在前端代码里（需等靶点库落地后移除） |
| — | 本项目本次改动（A-09 等）说明：**光看代码判断不了"已修好"，必须实跑**。建议补一条"批量链路冒烟测试"到 CI |

### 2.9 设计工作台 · 待后端（新功能，非缺陷）

| 项 | 状态 |
|---|---|
| 设计域库表（DDL `11_` ~ `25_`） | 仅设计文档，未建表 |
| `/api/design/**` 九个端点 | 契约已定（见 `README.md` 与 `docs/modules/design/`），后端未实现 |
| 分子生成器 S1~S6 | 待实现（确定性策略，**不需要新模型**） |
| 评估器接真实引擎 | 待实现；**必须走 `PipelineFactory`**，否则会污染 `predict_result` |
| 对接引擎 / ADMET | 需部署外部引擎；对应指标当前已置灰且权重按比例重分配 |
| 多智能体闭环 | 见 `docs/development/第二阶段开发文档-多智能体协同与智能体闭环.md` |

> 工作台**前端已可用**（四页签、2D 编辑器、3D 口袋视图、分子引擎实算均已跑通），
> 详见 `docs/modules/design/`（8 份）与 `log/CHANGELOG.md` v3.4.0。
> 对接构象依赖对接引擎，当前**未部署**；晶体结构中已有的共结晶配体则正常渲染。

---

## 3. 建议优先级

### 第一梯队 —— 有真实风险，且不需要数据

1. `B-01` 凭据轮换（**唯一已实际泄露风险**）
2. `B-02` 移除默认弱口令管理员
3. `B-03` / `B-04` / `B-12` 越权与 JWT 失效
4. `A-07` nginx `client_max_body_size`（一行，但直接影响批量上传）

### 第二梯队 —— 影响可用性/可信度

5. `C-01` / `C-05` 清除假数据（答辩/演示被追问就会暴露）
6. `D-02` 多格式输入切换控件（首页在宣传但不存在）
7. `D-03` / `D-04` 契约统一（`D-04` 已完成）
8. `B-15` / `B-16` 验证码冷却与邮箱归一化（大写 QQ 邮箱必失败）

### 第三梯队 —— 工程质量

9. `F-04` 移除 `DROP TABLE` 脚本 + 建立迁移机制
10. `F-06` 接入 CI（含 `vue-tsc` 与冒烟测试）
11. `G-18` 拆分巨型组件
12. `H-05` 补集成测试

### 等数据

13. 靶点库（`C-08` / `C-04` / `SRC-07`）与种子 SQL（`SRC-03`）

---

## 4. 一句话结论

**计算引擎（三算法 + 批量链路）已可用并实测验证。**
**干实验设计工作台前端已跑通**（四页签 + 2D 编辑器 + 3D 口袋视图 + 分子引擎实算），
其库表与 `/api/design/**` 仍待后端落地。
剩余问题里，**约 96 条不需要任何外部数据、随时可做**；
真正需要等测试数据的只有 6 条，且集中在靶点库这一块。
