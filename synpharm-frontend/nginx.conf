# =============================================================================
# SynPharm 前端 nginx 配置（nginx-unprivileged 非 root 运行，监听 8080）
#
# 架构说明：
#   - 前端静态资源由 nginx 直接提供
#   - /api/* 统一反代到 SpringBoot 后端（backend:8080）
#   - 不再直连 FastAPI（避免绕过 SpringBoot 的 JWT 鉴权）
# =============================================================================

server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # =========================================================================
    # 压缩
    # 设计工作台（/design）通过 iframe 加载分子编辑器子应用，其入口分块约 29 MB
    # （含 Indigo 化学引擎，WASM 被内联进 JS，无法拆成独立静态文件）。
    # 开启 gzip 后传输量约 8.5 MB，首屏体验差别明显。
    # =========================================================================
    gzip on;
    gzip_comp_level 5;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_vary on;
    gzip_types
        text/plain
        text/css
        text/javascript
        application/javascript
        application/json
        application/wasm
        image/svg+xml;

    # SPA 路由：所有非静态请求回退到 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 后端 API 反向代理（保留客户端真实 IP）
    location /api/ {
        proxy_pass http://backend:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 静态资源长缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
