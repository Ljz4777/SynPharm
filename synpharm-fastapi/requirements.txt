fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
# torch 由 Dockerfile 从 PyTorch CPU 索引单独安装（避免拉取 CUDA 依赖）
#
# numpy 统一到 2.x：项目的权重文件本身就是 numpy 2 时代产物，而 torch 2.5 与
#   transformers 5.x 也都要求 numpy 2。原先锁 1.26 会同时引发三个故障：
#   DDI 权重（numpy 2 存档）无法反序列化、PPI 因 torch 升不上去而不可用、
#   降级 transformers 时撞 numpy ABI 错误。
numpy>=2.1,<3
# pandas / scikit-learn 只在 DDI 离线训练脚本中使用，但必须同步抬高下限：
# 它们的旧 wheel 是按 numpy 1.x 编译的，在 numpy 2 下 import 即报 ABI 错误。
pandas>=2.2.3
scikit-learn>=1.5.2
python-multipart==0.0.6
# 三个算法真实推理所需依赖：
#   DDI-LLM / KAN-MoDTI 需要 rdkit（分子图 / 摩根指纹）
#   FlashPPI 需要 transformers + einops（trust_remote_code 加载 gLM2 骨架）
# rdkit 抬高下限：2024.03.1 起才提供针对 numpy 2 构建的 wheel（DTI 直接依赖它）
rdkit>=2024.3.1
# transformers 5.x 要求 torch>=2.5，对应 Dockerfile 里的 torch==2.5.1+cpu；
# 加 <6 上限，避免再次发生跨大版本静默漂移。
transformers>=5.0,<6
einops>=0.7
